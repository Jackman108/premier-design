/**
 * Данные для JSON-LD AggregateRating / блока доверия (не отдельный отзыв пользователя).
 */
export type StructuredDataAggregateRating = {
	ratingValue: string;
	reviewCount: number;
	bestRating: string;
};
