/**
 * Общие типы приложения (сквозные контракты). Типы пропсов фич — в каталогах `interface` рядом с компонентами.
 * Алиас: `@app-types/*` → `./types/*` (не `@types/*`, чтобы не конфликтовать с DefinitelyTyped).
 */
export {};
