# Открытый план аудита и улучшений (compliance с новой документацией)

Связанные документы: [`PROJECT_AUDIT_2026_04_RU.md`](PROJECT_AUDIT_2026_04_RU.md) · [`DEPLOY_READINESS_2026_04_RU.md`](DEPLOY_READINESS_2026_04_RU.md) · [`QUALITY_GATES_SYNC_RU.md`](QUALITY_GATES_SYNC_RU.md) · [`PROJECT_RISK_REGISTER_2026_04_RU.md`](PROJECT_RISK_REGISTER_2026_04_RU.md) · [`../../premier-design/CHANGELOG.md`](../../premier-design/CHANGELOG.md).

> Документ — **исполняемый бэклог**: только открытые пункты. После закрытия — запись в `CHANGELOG.md`, перенос в «Закрытые направления» в `PROJECT_AUDIT_2026_04_RU.md` или вычеркивание здесь. Исторические выполненные пункты в этот файл не возвращаются.

## Definition of Done (DoD)

Задача считается закрытой только если выполнены **все** условия:

1. Есть реальное изменение в коде или CI‑процессе (не только текстовая пометка).
2. Пройдены минимально требуемые гейты: для кода — `yarn check:static` минимум; для границ — `yarn check:risk:local`; для сборки/perf — `yarn check:precommit:full`.
3. Обновлены релевантные документы (`PROJECT_AUDIT_2026_04_RU.md`, `DEPLOY_READINESS_2026_04_RU.md`, `mempalace/rules/*`, ADR — по таблице синхронизации в `QUALITY_GATES_SYNC_RU.md`).
4. Есть запись в `premier-design/CHANGELOG.md` (`[Unreleased]`).
5. Для нетривиального решения — короткий комментарий «почему» в коде или ADR.

## Формат записи

```
| ID | Приоритет | Источник | Задача | Артефакт закрытия | Статус |
```

- **ID:** `<префикс>-XX`. Префиксы: `DOC` (документация/правила), `ARCH` (архитектура и границы), `QA` (тесты, гейты), `CI` (workflow, SLA), `PERF` (производительность), `SEC` (безопасность/supply‑chain), `OBS` (observability), `UI` (визуальный уровень), `DEV` (DX, скрипты).
- **Приоритет:** `P1` (блок merge/деплоя), `P2` (важно в текущем цикле), `P3` (опционально / next quarter).
- **Источник:** ссылка на правило/норму, из которой родилась задача.
- **Артефакт закрытия:** PR, файлы, команда, скриншот/прогон.
- **Статус:** `Open / In progress / Done`.

---

## 1. Compliance: `docs/mempalace/rules/` и `.cursor/rules/`

| ID | Приоритет | Источник | Задача | Артефакт закрытия | Статус |
|----|-----------|----------|--------|-------------------|--------|
| DOC-01 | P2 | `docs/mempalace/rules/01_*` + `02_*` (секция UI-слой) | Прогнать репозиторий по чек‑листу инвариантов: проверить, что в `*/ui/*` нет реализации хуков, объявлений типов и прямых IO‑вызовов. Точечный список нарушений — в issue/PR. | Список файлов‑нарушителей + правки + `yarn check:ui-purity` зелёный | Open |
| DOC-02 | P2 | `.cursor/rules/agent-quality-process.mdc` | Пройтись по `lint-staged` glob‑паттернам и убедиться, что они покрывают все слои (`shared/`, `widgets/`, `app/`, `pages/`, `lib/`); зафиксировать или дополнить. | diff `package.json` `lint-staged` + запись в `CHANGELOG` | Open |
| DOC-03 | P3 | `docs/mempalace/rules/01_*` + `08_*` | Сверить публичные API фич (`features/<slice>/index.ts`): все импорты «снаружи» — через баррель, без глубоких путей. Список нарушений — в issue. | grep отчёт + правки импортов | Open |

## 2. Архитектура и границы (ARCH)

| ID | Приоритет | Источник | Задача | Артефакт закрытия | Статус |
|----|-----------|----------|--------|-------------------|--------|
| ARCH-01 | P2 | `PROJECT_AUDIT_2026_04_RU.md` (FSD‑таблица) | Сократить allowlist архитектурных исключений (`scripts/architecture-allowlist.json`) ещё на N записей по плану roadmap; обновить `maxAllowedCount`. | `yarn check:architecture:progress` + diff allowlist | Open |
| ARCH-02 | P2 | `mempalace/rules/08_*` (use‑case + порты) | Ревизия use‑case под feedback и quiz: убедиться, что слой `application/useCases/` не знает про HTTP/JSON и **все** внешние зависимости — за функциями‑портами с явными типами. | список вызовов / refactor PR | Open |
| ARCH-03 | P3 | `mempalace/rules/01_*` | Запретить cross‑feature через `lib/` (finders разных слайсов): добавить explicit правило в `check-architecture-boundaries.mjs` либо тест‑sentinel. | обновлённый скрипт + тест | Open |

## 3. Качество, тесты, типы (QA)

| ID | Приоритет | Источник | Задача | Артефакт закрытия | Статус |
|----|-----------|----------|--------|-------------------|--------|
| QA-01 | P2 | `mempalace/rules/07_*` + новый `typecheck` | Включить шаг **`yarn typecheck`** в `ci.yml` (после `lint`, до `build`); синхронизировать `SCRIPTS_AND_QUALITY_GATES_RU.md` и `QUALITY_GATES_SYNC_RU.md`. | `.github/workflows/ci.yml` diff + CHANGELOG | Open |
| QA-02 | P2 | `mempalace/rules/07_*` (моки полным контрактом) | Аудит всех `jest.mock` в `**/__tests__/`: подтвердить, что моки хуков возвращают **полный** публичный return; покрыть тестом‑сторожем при наличии паттерна. | список файлов + правки | Open |
| QA-03 | P3 | `mempalace/rules/11_*` (Zod на границах) | Проверить, что **все** входы (`pages/api/*`, Server Actions, env) проходят через Zod до использования; отсутствующие схемы — добавить. | список API + новые валидаторы | Open |
| QA-04 | P3 | `guides/API_AND_STORYBOOK_RU.md` | Для каждого нового UI‑примитива в `shared/ui/primitives/*` — story с `autodocs` + базовый сценарий + a11y‑проверка. Закрыть «непокрытые» примитивы. | новые stories + `yarn build-storybook` | Open |

## 4. CI / стоимость / SLA (CI)

| ID | Приоритет | Источник | Задача | Артефакт закрытия | Статус |
|----|-----------|----------|--------|-------------------|--------|
| CI-01 | P2 | `audit/CI_COST_AND_TRENDS_RU.md` (RISK‑12) | Проверить актуальный p95 `CI Quality Gates` против `CI_SLA_P95_BUDGET_MIN`. При расхождении — пересчитать бюджет или вынести job. | артефакт `ci-trends-14d.md` + комментарий в issue | Open |
| CI-02 | P3 | `audit/SUPPLY_CHAIN_RU.md` (RISK‑09) | Включить `yarn audit --level critical` в основной `ci.yml` явным шагом (сейчас уже выполняется), синхронизировать формулировку в гайде. | подтверждение в `ci.yml` + правка гайда | Open |

## 5. Производительность (PERF)

| ID | Приоритет | Источник | Задача | Артефакт закрытия | Статус |
|----|-----------|----------|--------|-------------------|--------|
| PERF-01 | P2 | `guides/PERF_AND_SEO_CHECKLIST_RU.md` + `mempalace/rules/04_*` | Замер LCP/CLS/INP на проде (или превью), сверка с порогами `PERF_BUDGET_*`; при отклонениях — задача в `PROJECT_AUDIT_2026_04_RU.md`. | прогон Lighthouse + краткий отчёт | Open |
| PERF-02 | P3 | `mempalace/rules/04_*` | Аудит `dynamic(..., { ssr: false })` для тяжёлых клиентских модулей (карты/чат‑бот): соответствие реестру `@lib/dynamicSectionImports`. | список модулей + правки | Open |
| PERF-03 | P3 | ADR `0008` (`react-chatbot-kit`) | Подготовить план изоляции/замены при росте бандла или появлении CVE. | обновлённый ADR + RFC | Open |

## 6. Безопасность и данные (SEC)

| ID | Приоритет | Источник | Задача | Артефакт закрытия | Статус |
|----|-----------|----------|--------|-------------------|--------|
| SEC-01 | P2 | `mempalace/rules/05_*` + ADR `0004` | Сверить CSP (`script-src`, `img-src`, `connect-src`) при появлении новой аналитики или внешних ассетов; план — в ADR `0009`. | diff `next.config.js` + ADR | Open |
| SEC-02 | P2 | `mempalace/rules/05_*` (Zod, идемпотентность) | Сценарий «двойной сабмит» формы заявки: подтвердить тестом и (при необходимости) идемпотентным ключом. | unit/E2E тест | Open |
| SEC-03 | P3 | `audit/SUPPLY_CHAIN_RU.md` | Триадж текущего отчёта `report:audit:high`: список high‑severity → план снижения. | issue с триаджем + PR обновлений | Open |

## 7. Observability и надёжность (OBS)

| ID | Приоритет | Источник | Задача | Артефакт закрытия | Статус |
|----|-----------|----------|--------|-------------------|--------|
| OBS-01 | P2 | `audit/OPERATIONS_OBSERVABILITY_RU.md` (RISK‑11) | Пройти runbook RCA на тестовом инциденте feedback: `correlationId` end‑to‑end (UI → API → лог) ≤ 1 проход. | заметка в runbook + (при необходимости) правки логов | Open |
| OBS-02 | P3 | `mempalace/rules/10_*` | Аудит таймаутов и ретраев SMTP/Telegram (RISK‑10): подтверждение, что транзиенты обёрнуты в `retryAsync`, а истинные сбои — в `runWithIntegrationCircuit`. | список вызовов + комментарии «почему» | Open |

## 8. Визуальный уровень / UI (UI)

| ID | Приоритет | Источник | Задача | Артефакт закрытия | Статус |
|----|-----------|----------|--------|-------------------|--------|
| UI-01 | P2 | `mempalace/rules/02_*` (тёмная тема, токены) | Точечный визуальный аудит **тёмной темы** на страницах с медиа (offer/portfolio/services): подтвердить отсутствие комбинаций «светлый текст / светлая зебра» и контраст ≥ AA. | скриншоты + (при необходимости) правки токенов | Open |
| UI-02 | P3 | `mempalace/rules/03_*` (a11y) | Keyboard‑only smoke по всем модалкам (Estimate / Feedback / PhotoViewer / NewsModal): фокус‑лоdown, возврат на триггер, `Escape`. | заметка в `PROJECT_AUDIT_2026_04_RU.md` | Open |

## 9. DX и скрипты (DEV)

| ID | Приоритет | Источник | Задача | Артефакт закрытия | Статус |
|----|-----------|----------|--------|-------------------|--------|
| DEV-01 | P3 | новый `package.json` (упрощённая структура) | Для команд‑новичков — короткий «getting started» в `premier-design/README.md`: сценарии «первый день» (`yarn install` → `yarn dev` → `yarn check:static`) одним блоком. | обновлённый README | Open |
| DEV-02 | P3 | `guides/SCRIPTS_AND_QUALITY_GATES_RU.md` | Подумать над `yarn format(:check)` через Prettier (если введём): текущая политика — без Prettier, до отдельного решения (требует ADR + согласование). | ADR / решение «нет» | Open |
| DEV-03 | P3 | `agent-quality-process.mdc` | Добавить в репозиторий `CONTRIBUTING.md` (короткий, со ссылками на плейбук и правила) при росте команды. | новый файл | Open |

---

## Как добавлять новую задачу

1. Сформулировать строку по формату выше (ID, приоритет, источник правила, задача, артефакт, статус).
2. Поместить в соответствующий раздел (1–9). Если темы нет — завести новый раздел.
3. При закрытии: вычеркнуть из этого файла → запись в `CHANGELOG.md` → при необходимости перенести в «Закрытые направления» в `PROJECT_AUDIT_2026_04_RU.md`.

## Связь с реестром рисков

Задачи из этого плана могут быть связаны с конкретным `RISK-XX` в [`PROJECT_RISK_REGISTER_2026_04_RU.md`](PROJECT_RISK_REGISTER_2026_04_RU.md). При закрытии — обновлять колонку «Статус» / «Митигирующие действия» в реестре.
