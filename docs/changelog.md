# Changelog

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
версии по [Semantic Versioning](https://semver.org/lang/ru/).

## [Unreleased]

### Added

- **REF-PREM-01 / §9.2 портфельного аудита:** [`scripts/report-decomposition-threshold.mjs`](../scripts/report-decomposition-threshold.mjs), [`scripts/decomposition-allowlist.json`](../scripts/decomposition-allowlist.json), скрипт **`yarn report:decomposition-threshold`**; в **`yarn check:risk:local`** — **`--fail-on-violations`**. Правила: [`.cursor/rules/architecture-patterns.mdc`](../.cursor/rules/architecture-patterns.mdc); гайды: [`guides/scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md), [`guides/code-structure-and-naming-ru.md`](guides/code-structure-and-naming-ru.md), [`guides/cross-repo-rule-pack-ru.md`](guides/cross-repo-rule-pack-ru.md) §6; [`audit/quality-gates-sync-ru.md`](audit/quality-gates-sync-ru.md). Свод: [`cross-repo-portfolio-audit-2026-04-ru.md`](https://github.com/Jackman108/lendings-vps-infra/blob/master/docs/audit/cross-repo-portfolio-audit-2026-04-ru.md) §1.2.5, §9.2.
- **§6.2 портфельного аудита (BP-32 / BP-39 / BP-42 / BP-46):** [`src/app/opengraph-image.tsx`](../src/app/opengraph-image.tsx) (`next/og`, **`SITE_OPERATOR`**); [`.vscode/extensions.json`](../.vscode/extensions.json), [`.vscode/settings.json`](../.vscode/settings.json); smoke после SBOM в [`.github/workflows/ghcr-premium-design.yml`](../.github/workflows/ghcr-premium-design.yml); **`@axe-core/playwright`** — [`src/e2e/axe-a11y.spec.ts`](../src/e2e/axe-a11y.spec.ts) (`@extended`, прогон **`yarn test:e2e:extended`**).
- **§6.2 портфельного аудита (BP-02 / BP-11 / BP-16 / BP-17):** [`scripts/check-initial-js-budget.mjs`](../scripts/check-initial-js-budget.mjs) — опциональный потолок на один чанк **`INITIAL_JS_MAX_CHUNK_KB`** (CI **`1024`** KB); **`commitlint.config.mjs`**, [`.husky/commit-msg`](../.husky/commit-msg), скрипт **`yarn commitlint`** в [`package.json`](../package.json); расширенный **`GET /api/health`** ([`app/api/health/route.ts`](../src/app/api/health/route.ts)); распределённый rate limit — [`shared/lib/upstashRateLimit.ts`](../src/shared/lib/upstashRateLimit.ts), **`applyApiRateLimitWeb`** async ([`shared/lib/applyApiRateLimit.web.ts`](../src/shared/lib/applyApiRateLimit.web.ts)); [`.env.example`](../.env.example) — **`RATE_LIMIT_REDIS_REST_*`**. Исправлен путь **`yarn test:e2e:visual`** (`visual-regression.spec.ts` относительно **`testDir`**).
- **§6.1 портфельного аудита (perf + KPI, паритет febcode):** [`scripts/perf-local.mjs`](../scripts/perf-local.mjs), [`scripts/perf-prod.mjs`](../scripts/perf-prod.mjs), [`scripts/lib/perf-prod-url.mjs`](../scripts/lib/perf-prod-url.mjs), [`scripts/lib/perf-prod-fallback.mjs`](../scripts/lib/perf-prod-fallback.mjs), [`scripts/lib/script-logger.mjs`](../scripts/lib/script-logger.mjs), [`scripts/lib/run-powershell.mjs`](../scripts/lib/run-powershell.mjs), [`scripts/free-port-win.mjs`](../scripts/free-port-win.mjs); скрипты **`yarn perf:local`**, **`yarn perf:prod`**, **`yarn perf:prod:ci`** в [`package.json`](../package.json). Пост-релизный KPI: [`scripts/post-release-kpi-report.mjs`](../scripts/post-release-kpi-report.mjs), [`scripts/lib/kpi-report-constants.mjs`](../scripts/lib/kpi-report-constants.mjs), **`yarn kpi:post-release`** / **`yarn kpi:post-release:template`**. [`.gitignore`](../.gitignore) — **`.perf/`**. Документ [`guides/scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md).
- **§10 этап 10 (портфельный аудит / BP-30–31, BP-35–37, BP-29):** корневой [`SECURITY.md`](../SECURITY.md); [`.github/pull_request_template.md`](../.github/pull_request_template.md); [`.github/CODEOWNERS`](../.github/CODEOWNERS); [`.editorconfig`](../.editorconfig); расширенный [`.gitattributes`](../.gitattributes) (`text=auto eol=lf`, бинарные маски); в [`package.json`](../package.json) — **`packageManager`: `yarn@1.22.22`** (Corepack). Cron **[`.github/workflows/rule-pack-remote-health.yml`](../.github/workflows/rule-pack-remote-health.yml)** — три копии **`cross-repo-rule-pack-ru.md`** на GitHub доступны и не пусты (полное совпадение текста между репозиториями не требуется — разные вводные блоки). Сводный аудит — [`cross-repo-portfolio-audit-2026-04-ru.md`](https://github.com/Jackman108/lendings-vps-infra/blob/master/docs/audit/cross-repo-portfolio-audit-2026-04-ru.md) §10; **§10.4** (SemVer/changelog febcode) — только репозиторий **febcode**.
- **DATA-PREM-01 / BP-21:** cookie **`pd_site_locale`**, [`getCachedSiteBundle()`](../src/shared/lib/getStaticData.ts) (одно чтение локали + JSON на запрос), [`proxy.ts`](../proxy.ts) → заголовок **`x-pathname`** (Next.js 16+: вместо устаревшего соглашения **`middleware`**), [`hreflangAlternatesForPath`](../src/shared/lib/seo/hreflangAlternates.ts) + OpenGraph **`locale`** / **`alternateLocale`** в корневом **`generateMetadata`** [`app/layout.tsx`](../src/app/layout.tsx), атрибут **`lang`** на **`<html>`**; Server Action [`set-site-locale.ts`](../src/shared/lib/server-actions/set-site-locale.ts); переключатель [`SiteLocaleSwitcher`](../src/widgets/layout/ui/site-locale-switcher/SiteLocaleSwitcher.tsx); клиентский слой **[`shared/i18n/`](../src/shared/i18n/)** (`LocaleProvider`, `useLocale`), словари **[`data/locales/*/ui.json`](../src/data/locales/README.md)**; **`buildLayoutProps`** без проброса локали в header/footer; XML **`GET /api/sitemap`** — канон **`ru`**. Тесты: [`resolve-request-site-locale.test.ts`](../src/shared/site-data/__tests__/resolve-request-site-locale.test.ts); контракт **`feedbackSchema`** (BP-15) — блок в [`features/feedback/__tests__/schema.test.ts`](../src/features/feedback/__tests__/schema.test.ts) (не импорт **`shared`** → **`@features`**).
- **§9 этап 9 (портфельный аудит / BP-21 инфра, BP-24, BP-48):** [`widgets/home-page-section/config/home-page-slot-registry.tsx`](../src/widgets/home-page-section/config/home-page-slot-registry.tsx) — **`HOME_PAGE_REGISTERED_SLOTS`** в паре с [`home-page-slots.ts`](../src/widgets/home-page-section/config/home-page-slots.ts), dev-ассерт выравнивания; [`HomePageSection`](../src/widgets/home-page-section/ui/HomePageSection/HomePageSection.tsx) — атрибут **`data-slot-id`**. Локали: [`shared/site-data/site-locale.ts`](../src/shared/site-data/site-locale.ts), [`loadSiteData(locale)`](../src/shared/site-data/load-site-data.ts), каталог [`data/locales/en/`](../src/data/locales/en/README.md) (канон продакшена — **`ru`**). ESLint: **`widgets/**/_`** + **`pages-layer/\*\*/_`** — **`no-restricted-imports`** (`@features/_/**`); публичные реэкспорты в **`features/_/index.ts`** (`ServiceCategoryPageProps`, `RelatedServiceDetailProps`, `useShareBanner`, `ChatBotSidebar`). Сводный аудит — [`cross-repo-portfolio-audit-2026-04-ru.md`](https://github.com/Jackman108/lendings-vps-infra/blob/master/docs/audit/cross-repo-portfolio-audit-2026-04-ru.md) §9 этап 9.
- **§9 этап 6 (портфельный аудит / BP-48, BP-15):** единый **`feedbackSchema`** (`@shared/validates` ↔ `@features/feedback/schema`) — см. блок контракта в [`features/feedback/__tests__/schema.test.ts`](../src/features/feedback/__tests__/schema.test.ts). В [`eslint.config.mjs`](../eslint.config.mjs) для **`pages-layer/**/_`** — **`no-restricted-imports`** (`@features/_/\*\*`). Гайд [`guides/scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md).
- **§9 этап 5 (портфельный аудит / Web Vitals):** [`shared/ui/web-vitals/WebVitalsReporter.tsx`](../src/shared/ui/web-vitals/WebVitalsReporter.tsx) в [`root-providers.tsx`](../src/app/root-providers.tsx) + **`POST`** [`app/api/vitals/route.ts`](../src/app/api/vitals/route.ts) + схема [`shared/lib/web-vitals-ingest.ts`](../src/shared/lib/web-vitals-ingest.ts); опционально **`WEB_VITALS_LOG=1`** в [`.env.example`](../.env.example). Ошибки React — по-прежнему **`console.error`** в [`AppErrorBoundary`](../src/shared/ui/error-boundary/AppErrorBoundary.tsx) (без внешнего SaaS).
- **BP-34 / спринт S1 (портфельный аудит):** [`validateStartupEnv.ts`](../src/services/validateStartupEnv.ts) (Zod) — в **production** обязательны **`TELEGRAM_BOT_TOKEN`** и **`TELEGRAM_CHAT_ID`**; при заданном **`EMAIL_HOST`** — полный набор SMTP env; проверка отключена в **`GITHUB_ACTIONS`** (как в CI). Вызов из [`app/layout.tsx`](../src/app/layout.tsx); тесты [`services/__tests__/validateStartupEnv.test.ts`](../src/services/__tests__/validateStartupEnv.test.ts).
- **BP-34 (доп.):** `validateStartupEnv` не блокирует **`next build`** / образ (`NEXT_PHASE` compile, `SKIP_STARTUP_ENV_VALIDATION` в `Dockerfile.prod`); проверка TELEGRAM\_\* остаётся на рантайме **`next start`**. Тесты и порядок импортов в [`app/layout.tsx`](../src/app/layout.tsx).
- **§9 этап 1.3 / единый JSON API (портфель = febcode эталон):** [`createApiErrorPayload`](../src/shared/lib/api/apiRequestRuntime.ts) возвращает **`{ success: false, errorCode, error, correlationId }`**; **`POST /api/feedback`** — успех **`{ success: true, correlationId, message }`**; **`GET /api/sitemap`** — JSON-ошибки в том же контракте + **`X-Correlation-Id`**. Клиент [`postFeedbackClient.ts`](../src/shared/lib/postFeedbackClient.ts) читает **`error`**, иначе legacy **`message`**. Правило [`.cursor/rules/api-conventions.mdc`](../.cursor/rules/api-conventions.mdc); тесты/e2e обновлены.

### Fixed

- **`.husky/pre-push`:** убран устаревший **`cd premier-design`** после переноса приложения в корень клона (**ARCH-PREM-01**) — иначе **`git push`** падал с «No such file or directory».
- **Playwright / `yarn test:e2e`:** выбор **`yarn start:standalone`** только при **`GITHUB_ACTIONS=true`** (локально **`CI=1` в shell** больше не поднимает prod без **`TELEGRAM_*`**); в workflow подставляются placeholder **`TELEGRAM_BOT_TOKEN`** / **`TELEGRAM_CHAT_ID`**. [`playwright.config.ts`](../playwright.config.ts)
- **e2e / keen-slider `loop`:** флаг **`NEXT_PUBLIC_PLAYWRIGHT_WEB_E2E=1`** (веб-сервер Playwright + **`yarn build`** в **e2e** / **e2e-extended** workflows) отключает **loop** в [`useSliderState.ts`](../src/shared/ui/slider/hooks/useSliderState.ts) — иначе DOM-клоны слайдов без React не открывают `PhotoViewer`. В [`playwright.config.ts`](../playwright.config.ts) **`reuseExistingServer: false`**; локально перед **`yarn dev`** — удаление **`.next`** + **`cross-env …`** (иначе Turbopack / кеш без публичного env).
- **e2e extended:** фото на главной — **`getByRole('button', { name: /Zhlobin|Жлобин/i })`**, **focus + Enter**, **`dialog[open]`** до **15 s**; у карточки примеров явный **`aria-label={card.address}`**. [`src/e2e/smoke.spec.ts`](../src/e2e/smoke.spec.ts), [`ExampleCard.tsx`](../src/features/examples/ui/ExamplesCard/ExampleCard.tsx)
- **a11y / контраст футера (axe):** даты блока новостей и подписи **`dt`** реквизитов — **`--beige-dark`** на **`--brown-dark`**. [`footerNews.module.css`](../src/features/news/ui/News/footerNews.module.css), [`LegalRequisites.module.css`](../src/shared/ui/legal-requisites/LegalRequisites.module.css)
- **`perf:local` без `.env`:** подъём standalone с **`NODE_ENV=production`** больше не падает на **`validateStartupEnv()`** ( **`SKIP_STARTUP_ENV_VALIDATION=1`** для дочернего процесса; отключение — **`PERF_LOCAL_STRICT_ENV=1`**). [`scripts/perf-local.mjs`](../scripts/perf-local.mjs), [`guides/scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md).
- **`perf:local` на Windows:** [`scripts/perf-local.mjs`](../scripts/perf-local.mjs) — дочерний Node через **`execFileSync`**; **`yarn build`** через **`shell: true`** и одну командную строку (резолв **`yarn.cmd`**); проверка **`run.error`**. [`.prettierignore`](../.prettierignore) — **`.perf`** для Lighthouse.

### Changed

- **DATA-PREM-01 / BP-21 / §9.2 свода:** базовая редакционная вычитка EN для [`data/locales/en/data.json`](../src/data/locales/en/data.json) — **`menu[].ruTitle`** совпадает с русскими подписями из **`ru/data.json`** (контракт [`Menu.props.ts`](../src/shared/ui/menu/interface/Menu.props.ts)); правки блоков hero / trust signals / video spotlight; [`data/locales/en/README.md`](../src/data/locales/en/README.md); комментарий в [`site-locale.ts`](../src/shared/site-data/site-locale.ts). Тест парсинга EN-бандла — [`dataPropsSchema.test.ts`](../src/shared/validates/__tests__/dataPropsSchema.test.ts). Свод: [`cross-repo-portfolio-audit-2026-04-ru.md`](https://github.com/Jackman108/lendings-vps-infra/blob/master/docs/audit/cross-repo-portfolio-audit-2026-04-ru.md) §9.2.
- **`docs/README.md`:** раздел «Портфель и соседние репозитории» (единая таблица: rule pack, alignment, свод §9.2, VPS); сокращены дубли про Feb Code в оглавлении гайдов.
- **Prettier / качество:** [`.prettierignore`](../.prettierignore) — **`test-results`**, **`playwright-report`**, **`blob-report`** (артефакты e2e не участвуют в **`format:check`**). **`yarn format`** — выравнивание затронутых файлов под **`yarn check:static`**.
- **`guides/cross-repo-rule-pack-ru.md`:** §1 — строка **`yarn test:e2e:extended`** (паритет **febcode**); §2 — **A** (pre-commit): **`lint-staged`** + **`yarn typecheck`**, дословный паритет с **febcode** / **lendings-vps-infra**; §6 — ссылка на **§6.2.1** / **§6.2.2** сводного аудита и **BP-12** / **BP-04**, справка про контент лендинга **febcode** после **FB-R-06** (`entities/<slice>/content`, `entities/site-data`, фасад `@shared/site-content`); **Обновлено** — синхрон с **febcode** (§1–§6). Свод: [`cross-repo-portfolio-audit-2026-04-ru.md`](https://github.com/Jackman108/lendings-vps-infra/blob/master/docs/audit/cross-repo-portfolio-audit-2026-04-ru.md) §3.2.
- **Структура репозитория (ARCH-PREM-01):** корень клона — **`package.json`**, **`yarn.lock`**, **`next.config.js`**, Docker, Storybook, **`scripts/`**, **`public/`** (как **febcode**); исходники Next.js — **`src/`**. CI и **GHCR** без `working-directory`/`./premier-design`; **Dependabot** npm — **`/`**. Обновлены **`.husky/pre-commit`**, гайды и **`globs`** в `.cursor/rules/`. Завершение переноса: удалены дубликат-папка **`premier-design/premier-design`**, локальный **`src/node_modules`**, неиспользуемые **`vitest.shims.d.ts`** и легаси **`.eslintrc.json`** (канон — **`eslint.config.mjs`**); исправлен импорт глобальных стилей в **`.storybook/preview.ts`** (`src/widgets/styles/globals.css`).
- **Игноры артефактов:** [`.gitignore`](../.gitignore) — явные **`src/coverage/`**, **`src/.next/`**, **`.vercel`**, сгруппированные блоки без дублей lighthouse/storybook; [`.dockerignore`](../.dockerignore) и [`.prettierignore`](../.prettierignore) — те же пути (**`src/coverage`**, **`src/.next`**, **`.vercel`**).
- **TypeScript 6 / `tsconfig`:** из [`tsconfig.paths.json`](../tsconfig.paths.json) удалён **`baseUrl`** (в TS 6 опция помечена как deprecated до 7.0; см. [aka.ms/ts6](https://aka.ms/ts6)). Алиасы **`paths`** с **`./...`** резолвятся от каталога конфигурации — **`yarn typecheck`** без **`ignoreDeprecations`** в [`tsconfig.json`](../tsconfig.json). В [`tsconfig.jest.json`](../tsconfig.jest.json) — **`moduleResolution`: `bundler`**; в [`jest.config.cjs`](../jest.config.cjs) для **ts-jest** задано **`diagnostics.ignoreCodes: [5101, 5107]`**, чтобы сборка тестов не падала на deprecated-диагностиках внутреннего мерджа опций (без **`ignoreDeprecations`** в JSON, чтобы не ломать языковой сервис на TS 5.x).
- **Тесты и статика после плоского корня:** импорты **`public/*.svg`** из [`Logo.tsx`](../src/shared/ui/logo/Logo.tsx), [`ArrowButton.tsx`](../src/shared/ui/arrow-button/ArrowButton.tsx) и `jest.mock` в тестах — **`../../../../public/...`**; [`lib-cross-feature-sentinel.test.ts`](../src/tests/architecture/lib-cross-feature-sentinel.test.ts) ищет фичи в **`src/features`**.
- **§5.2 cross-repo audit (правила):** [`.cursor/rules/architecture-patterns.mdc`](../.cursor/rules/architecture-patterns.mdc) — явный ориентир **pure view** (≤ ~200 строк, хуки/типы вне view); [`.cursor/rules/docs-standards.mdc`](../.cursor/rules/docs-standards.mdc) — запрет новых `docs/**/*.md` без необходимости (паритет febcode). [`guides/cross-repo-rule-pack-ru.md`](guides/cross-repo-rule-pack-ru.md) — уровень **A** с **`yarn typecheck`** в таблице.
- **CI / качество:** [`README.md`](../README.md) — относительные ссылки на документы с корня репозитория (**`./docs/...`**, **`./LICENSE`** и т.д.), явные файлы вместо каталогов для markdown-link-check; в [`changelog.md`](changelog.md) исправлены пути к скриптам (**`../scripts/`**, не **`../src/scripts/`**). Lighthouse: порог **`PERF_BUDGET_CLS`** **0.17** ([`check-lighthouse-budget.mjs`](../scripts/check-lighthouse-budget.mjs), [`ci.yml`](../.github/workflows/ci.yml)). GHCR: [`.trivyignore`](../.trivyignore) для **CVE-2026-33671** (picomatch в npm базового образа `node:24-alpine`, не рантайм приложения) + `trivyignores` в [`ghcr-premium-design.yml`](../.github/workflows/ghcr-premium-design.yml). [`codeql.yml`](../.github/workflows/codeql.yml): **`upload: false`**, пока в репозитории не включён GitHub Code scanning (иначе падает загрузка SARIF).
- **Документация:** удалены дублирующие гайды `testing-standards-cross-repo-ru.md` и `prettier-and-formatting-cross-repo-ru.md`; блоки перенесены в [`guides/scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md).
- **`package.json` и rule pack:** поле **`scripts`** упорядочено по матрице **febcode** (dev → build → format/lint → тесты → гейты → e2e → storybook); **`build:analyze`** + **`analyze`**. [`cross-repo-rule-pack-ru.md`](guides/cross-repo-rule-pack-ru.md) §1–§5 дословно с **febcode**; полный локальный контур — **`yarn ci:quality`** (алиасы **`check:precommit:full`** / **`check:deploy:local`** убраны из **premier-design** и **febcode**). [`scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md), [`README.md`](../README.md), шаблон PR, [`rule-pack-remote-health.yml`](../.github/workflows/rule-pack-remote-health.yml).
- **Сводный портфельный аудит (`lendings-vps-infra`):** **§9.1** архив этапов 1–10, **§9.2** единая очередь открытых задач; [`audit/audit-and-improvement-plan-ru.md`](audit/audit-and-improvement-plan-ru.md) — отсылка к **§9.2** для кросс-репо планирования.
- **CI / документация:** ссылки на **`lendings-vps-infra`** в `docs/` переведены на **`https://github.com/Jackman108/lendings-vps-infra/blob/master/...`** (проход **`markdown-link-check`** без соседнего клона). GHCR workflow — **`aquasecurity/trivy-action@v0.36.0`** (тег с префиксом **`v`**).
- **DATA-PREM-01 / UI-i18n:** [`LocaleProvider`](../src/shared/i18n/LocaleProvider.tsx) в [`root-providers.tsx`](../src/app/root-providers.tsx) с **`initialSiteLocale`** из layout; **`siteLocale`** не пробрасывается через **`buildLayoutProps`** — меню и переключатель берут локаль из **`useLocale`**; строки интерфейса — только из **`data/locales/*/ui.json`**.
- **DATA-PREM-01:** страницы и loaders переведены на **`getCachedSiteBundle()`** вместо **`Promise.all([getCachedData(), resolveRequestSiteLocale()])`**; **`generateMetadata`** корневого layout вместо статического **`metadata`** (слияние с hreflang).
- **§9 этап 9 / BP-48:** [`eslint.config.mjs`](../eslint.config.mjs) — правило **`no-restricted-imports`** для **`widgets/**/\*`** (раньше только **`pages-layer`**); импорты фич из виджетов — только **`@features/<slice>`**. [`guides/scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md).
- **§9 этап 6:** [`pages-layer/home/HomePageClient.tsx`](../src/pages-layer/home/HomePageClient.tsx) — тип **`HeroBannerProps`** с **`@features/banner`** (публичный API вместо deep-import).
- **§9 этап 7 (BP-28):** [`.github/workflows/docs-markdown-links.yml`](../.github/workflows/docs-markdown-links.yml) + [`.markdown-link-check.json`](../.markdown-link-check.json) — `markdown-link-check` по всем tracked **`*.md`**; игнор **`mailto:`**, npm advisories (403), путей **`lendings-vps-infra`** (CI без соседнего клона). Каталог **`deploy/`** убран; деплой описан в [`guides/deploy-vercel-and-vps-ru.md`](guides/deploy-vercel-and-vps-ru.md). Правки битых относительных ссылок в [`changelog.md`](changelog.md), [`guides/layer-imports-and-public-api-cross-repo-ru.md`](guides/layer-imports-and-public-api-cross-repo-ru.md).
- **Dev-скрипты:** переименование **`dev:turbo` → `dev:webpack`** в [`package.json`](../package.json) (имя совпадает с бандлером); обновлены гайды и [`Dockerfile.dev`](../Dockerfile.dev) (комментарий к `CMD`).
- **§9 этап 4 (портфельный аудит / supply-chain):** [`.github/dependabot.yml`](../.github/dependabot.yml) (npm `/` + github-actions); [`.github/workflows/codeql.yml`](../.github/workflows/codeql.yml) (CodeQL JS/TS); [`.github/workflows/ghcr-premium-design.yml`](../.github/workflows/ghcr-premium-design.yml) — **Trivy** (HIGH/CRITICAL до push) + **SBOM** CycloneDX (artifact). Обновлены [`audit/quality-gates-sync-ru.md`](audit/quality-gates-sync-ru.md), [`guides/scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md).
- **§9 этап 3 (портфельный аудит):** переименованы [`audit/project-audit-ru.md`](audit/project-audit-ru.md), [`audit/deploy-readiness-ru.md`](audit/deploy-readiness-ru.md) (long-living); обновлены перекрёстные ссылки в репозитории. [`audit/audit-and-improvement-plan-ru.md`](audit/audit-and-improvement-plan-ru.md) — таблица PART из §1.1 (трассировка), дата в шапке. **ADR 0014** — [`adr/0014-fsd-entities-slice-and-public-api.md`](adr/0014-fsd-entities-slice-and-public-api.md); индекс [`adr/README.md`](adr/README.md); [`.cursor/rules/frontend-fsd.mdc`](../.cursor/rules/frontend-fsd.mdc) — явные ссылки на **ADR 0014** / **0012**. Dev: **`yarn dev`** — **Turbopack**, **`yarn dev:webpack`** — **Webpack** ([`package.json`](../package.json), [`guides/scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md), [`guides/cross-repo-rule-pack-ru.md`](guides/cross-repo-rule-pack-ru.md), [`README.md`](../README.md)).
- **§9 этап 2.2 (портфельный аудит):** [`.husky/pre-commit`](../.husky/pre-commit) — только **`lint-staged`** + **`yarn typecheck`** (без полного контура на каждый коммит); полная цепочка — **`yarn ci:quality`** / CI. [`.cursor/rules/agent-quality-process.mdc`](../.cursor/rules/agent-quality-process.mdc), [`guides/scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md), [`guides/cross-repo-rule-pack-ru.md`](guides/cross-repo-rule-pack-ru.md) — уровни A/B.
- **§5 сводного аудита (`lendings-vps-infra`):** атомарные правила Cursor — `.cursor/rules/project-overview.mdc`, `frontend-fsd.mdc`, `architecture-patterns.mdc`, `react-patterns.mdc`, `api-conventions.mdc`, `testing.mdc`, `docs-standards.mdc`; перекрёстная отсылка в `agent-quality-process.mdc`.
- **Node (паритет портфеля):** [`.nvmrc`](../.nvmrc) зафиксирован на **24.14.1**; в [`package.json`](../package.json) поле **`engines.node`**: **`>=24.14.1 <25`** (как в febcode и локальной среде CI).
- **E2E smoke:** тест «lead modal opens from hero CTA» кликает CTA внутри **`#home-hero`** (не **`.first()`** среди всех «Сделать заказ» на странице), ждёт **`#feedback-modal-heading`**, затем **`#phone`** (**`PatternFormat`** не всегда **`role=textbox`** в Playwright).
- **E2E / feedback API:** в **`playwright.config.ts`** для **`CI=true`** вместо **`yarn start`** используется **`node .next/standalone/server.js`** (совместимо с **`output: 'standalone'`**). В **`app/api/feedback/route.ts`** добавлен **`GET`** с ответом **405** и JSON **`{ message: '…Method not allowed…' }`** — smoke-тест не падает на пустом теле ответа.
- **Quality gates / perf:** `scripts/check-initial-js-budget.mjs` приведён к **Next.js 15+ App Router**: если в `build-manifest.json` нет чанков для `/`, считается оболочка (**`polyfillFiles` + `rootMainFiles`**). В **`ci:quality`** добавлен **`yarn format:check`** в начало цепочки (паритет с **`check:static`** и CI). В **`.prettierignore`** добавлен каталог **`.audit/`** (сводки аудита не должны ломать **`yarn format:check`**).
- **ADR 0013 (`shared/lib`, React Query, Prettier):** корень **`lib/`** перенесён в **`shared/lib/`** — импорты **`@shared/lib/...`** вместо **`@lib/*`**; архитектурный скрипт считает **`shared/lib/`** исключением для импортов `@features/*`. **`@tanstack/react-query`**, фабрика **`createQueryClient`** ([`shared/lib/query/react-query-config.ts`](../src/shared/lib/query/react-query-config.ts)), **`QueryClientProvider`** в [`root-providers.tsx`](../src/app/root-providers.tsx). Prettier, **`eslint-config-prettier`**, [`.prettierrc.json`](../.prettierrc.json), скрипты **`yarn format`** / **`yarn format:check`**; **`check:static`** и **`ci:quality`** включают **`format:check`**. Документы: [`adr/0013-shared-lib-react-query-prettier.md`](adr/0013-shared-lib-react-query-prettier.md), [`guides/scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md).
- **PD-R-06 (данные, RU-only):** контент лендинга — **`data/locales/ru/data.json`** (перенос из `data/data.json`); загрузка — **`loadSiteData()`** в **`@shared/site-data`** + **`dataPropsSchema`**; локаль контента — только **`SiteLocale = 'ru'`**. Дев-файл **`formData.json`** — **`formDataPersistenceSchema`**; **`fileService`** — **`readPersistedFeedback`**. Изначально тексты ошибок формы — **`@shared/i18n/messages/feedback-form`**; **актуально** — **`data/locales/*/ui.json`**, типизированные ключи **`UI`**, **`@shared/i18n`**. См. **`data/locales/README.md`**, [`code-structure-and-naming-ru.md`](guides/code-structure-and-naming-ru.md).
- **Паритет роутинга с Feb Code:** алиасы **`@app`**, **`@app/*`**, **`@pages`** (без `/*`) в [`tsconfig.paths.json`](../tsconfig.paths.json) и [`jest.config.cjs`](../jest.config.cjs); главная — тонкий [`app/page.tsx`](../src/app/page.tsx) + [`pages-layer/home/home-metadata.ts`](../src/pages-layer/home/home-metadata.ts) / [`home-route.tsx`](../src/pages-layer/home/home-route.tsx) (**`revalidate`** только в `app/page.tsx` — Next.js не поддерживает реэкспорт segment config). ESLint: исключение `no-restricted-imports` для **`app/`** и **`pages-layer/`** (вместо удалённого `pages/`); в **`features/`** запрет импорта **`@pages/**`** и **`@pages-layer/**`**. Обновлены [`docs/guides/layer-imports-and-public-api-cross-repo-ru.md`](guides/layer-imports-and-public-api-cross-repo-ru.md), [`AGENTS.md`](../AGENTS.md), сводный аудит в **`lendings-vps-infra`** — [`cross-repo-portfolio-audit-2026-04-ru.md`](https://github.com/Jackman108/lendings-vps-infra/blob/master/docs/audit/cross-repo-portfolio-audit-2026-04-ru.md).
- **Маршрутизация Next.js (PD-R-07 → полный App Router):** приложение на **едином App Router** (`app/`): маркетинговые страницы и `/services/...` — RSC + `generateMetadata` / `StructuredDataScript` ([`customHeadPropsToMetadata.ts`](../src/shared/lib/seo/customHeadPropsToMetadata.ts)); JSON-LD без `next/head` ([`StructuredDataScript`](../src/widgets/layout/seo/StructuredDataScript.tsx)); API — [`app/api/feedback/route.ts`](../src/app/api/feedback/route.ts), [`app/api/sitemap/route.ts`](../src/app/api/sitemap/route.ts); каталог **`pages/`** удалён. Алиасы **`@pages/*`** → [`pages-layer/`](../src/pages-layer/) и **`@pages-layer/*`** в [`tsconfig.paths.json`](../tsconfig.paths.json) / [`jest.config.cjs`](../jest.config.cjs). Главная — [`pages-layer/home/HomePageClient.tsx`](../src/pages-layer/home/HomePageClient.tsx). Навигация и fallback — **`next/navigation`** ([`useBackNavigation`](../src/shared/ui/back-button/hooks/useBackNavigation.ts), [`useFallback`](../src/shared/hooks/useFallback.tsx)). Загрузчики и `generateStaticParams` — каталог [`shared/lib/app-router/`](../src/shared/lib/app-router/loadServiceDetailPage.ts).
- **PD-R-05 (продолжение):** слой [`entities/`](../src/entities/index.ts) расширен срезами **`banner`**, **`button`**, **`title`**, **`news`**, **`costing`**, **`panel`**, **`share-banner`**, **`appeal`**, **`seo`** (JSON-LD DTO); из [`shared/interface/`](../src/shared/interface/) удалены дубли (`NewsItem.props.ts`, `BannerImage.props.ts`, `Button.props.ts`, `Panel.props.ts`, `CostingCard.props.ts`, `ShareBannerData.props.ts`, `appealSectionData.props.ts`). [`Title.props.ts`](../src/shared/ui/title/interface/Title.props.ts) реэкспортирует **`TitleProps`** из `@entities/title`; [`seoHead.props.ts`](../src/shared/interface/seoHead.props.ts) импортирует **`FaqStructuredDataItem`** / **`ServiceJsonLdInput`** из `@entities/seo`; [`generateStructuredData.ts`](../src/shared/lib/seo/generateStructuredData.ts) и [`CustomHead.props.ts`](../src/widgets/layout/seo/CustomHead/CustomHead.props.ts) обновлены под те же источники.
- **PD-R-09 / PD-R-10 / PD-R-04 (кросс-репо аудит §3.1):** JSON-LD для `<CustomHead>` перенесён в [`shared/lib/seo/generateStructuredData.ts`](../src/shared/lib/seo/generateStructuredData.ts); зафиксирован ADR [`0012-root-services-folder-vs-shared-api.md`](adr/0012-root-services-folder-vs-shared-api.md); в MemPalace **08** добавлен блок «Эталон в кодовой базе» (`features/feedback/useCases`).
- **PD-R-01 / PD-R-02 / PD-R-03 (кросс-репо аудит §3.1):** SSG-хелперы перенесены в [`shared/lib/staticProps/findService.ts`](../src/shared/lib/staticProps/findService.ts) (`findService`, `getCommonProps`); главная страница — композиция [`HomePageSections`](../src/widgets/home-page-section/config/HomePageSections.tsx) + реестр [`HOME_PAGE_SECTION_IDS`](../src/widgets/home-page-section/config/home-page-slots.ts); константы оператора разнесены по [`shared/constants/company/index.ts`](../src/shared/constants/company/index.ts) (`types`, `social`, `operator`, `developer`), публичный импорт по-прежнему `@shared/constants/company`.
- **Артефакты аудита и наблюдаемости:** сводки `yarn audit` (high), Lighthouse perf и файл событий feedback SLO выводятся в **`.audit/`** (каталог в `.gitignore`), а не в точечные файлы в корне приложения; обновлены [`security-high-weekly.yml`](../.github/workflows/security-high-weekly.yml), [`scripts/report-yarn-audit-high.mjs`](../scripts/report-yarn-audit-high.mjs), [`scripts/check-lighthouse-budget.mjs`](../scripts/check-lighthouse-budget.mjs), [`scripts/check-feedback-slo.mjs`](../scripts/check-feedback-slo.mjs), [`shared/lib/feedbackSlo.ts`](../src/shared/lib/feedbackSlo.ts).
- **Кросс-репо нэйминг (febcode):** русскоязычные гайды febcode унифицированы под суффикс `-ru.md` (`architecture-ru.md`, `docker-ru.md`, `testing-standards-ru.md`, `deploy-checklist-ru.md`, `ab-experiments-playbook-ru.md`, `kpi-post-release-report-ru.md`, `cross-repo-alignment-plan-ru.md`, `project-audit-ru.md`, `roadmap-ru.md`, `style-audit-ru.md`). Соответственно обновлены кросс-репо ссылки в `docs/audit/cross-repo-alignment-ru.md`, `docs/guides/cross-repo-rule-pack-ru.md`, `docs/guides/layer-imports-and-public-api-cross-repo-ru.md`, `docs/guides/feb-code-post-release-sync-checklist-ru.md`, `docs/guides/scripts-and-quality-gates-ru.md`.
- **Кросс-репозиторное качество:** [`guides/cross-repo-rule-pack-ru.md`](guides/cross-repo-rule-pack-ru.md) — единая лестница гейтов (A–D: pre-commit → local → PR CI → nightly/deploy), границы унификации и таблица канонических источников для Premier Design (синхронные копии в febcode и lendings-vps-infra).
- **Кросс-репозиторный аудит:** закрыты **DOC-06**, **CI-04**, **DEV-05** — см. [`guides/cross-repo-rule-pack-ru.md`](guides/cross-repo-rule-pack-ru.md); перечень закрытий и ссылки на артефакты в [`audit/audit-and-improvement-plan-ru.md`](audit/audit-and-improvement-plan-ru.md); [`audit/cross-repo-alignment-ru.md`](audit/cross-repo-alignment-ru.md) обновлён под синхронный словарь деплоя и rule pack для трёх репозиториев.
- **Документация:** ссылки на multi-site VPS теперь ведут в `lendings-vps-infra/docs/operations/multisite-vps-deploy-ru.md`, так как `docs/operations/` в этом репозитории не является источником compose/nginx.
- **Документация:** все файлы в `docs/` переименованы в **kebab-case** (латиница, дефисы; русскоязычные материалы — суффикс `-ru.md`); обновлены перекрёстные ссылки в репозитории и во внешних указателях (febcode, lendings-vps-infra). Исключение по традиции: `README.md`.
- **Документация:** журнал изменений перенесён из `premier-design/CHANGELOG.md` в [`docs/changelog.md`](changelog.md); все ссылки в репозитории обновлены на единый канон «история в `docs/`».
- **Документация (кросс-репо C3):** в [`guides/scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md) уточнён путь к канону Feb Code — `docs/guides/testing-standards-ru.md` (отдельный репозиторий).

### Added

- **`GET /api/health`:** [`app/api/health/route.ts`](../src/app/api/health/route.ts) — лёгкий JSON (`status`, `version`, `timestamp`), **`Cache-Control: no-store`**; контракт с **`lendings-vps-infra`** (`docker-compose`: healthcheck не дергает главную страницу); паритет с Feb Code (`febcode/src/app/api/health/route.ts`).
- **PD-R-05 (портфельный аудит §3.1):** слой **`entities/`** — [`premier-design/entities/`](../src/entities/index.ts): `service`, `document`, `review`; алиасы `@entities` в [`tsconfig.paths.json`](../tsconfig.paths.json), [`jest.config.cjs`](../jest.config.cjs). Доменные типы **только** в `entities`, без дублей в `shared/interface` (удалены совместимые заглушки `CategoryPrice.props.ts`, `PaperItem.props.ts`; рейтинг JSON-LD — импорт из `@entities/review`, без реэкспорта из [`seoHead.props.ts`](../src/shared/interface/seoHead.props.ts)). Документация: [`guides/code-structure-and-naming-ru.md`](guides/code-structure-and-naming-ru.md), [`mempalace/rules/01-web-architecture-and-boundaries-ru.md`](mempalace/rules/01-web-architecture-and-boundaries-ru.md).
- **BP-27 (кросс-репо аудит §6.2):** корневой [`AGENTS.md`](../AGENTS.md) — точка входа для агента (структура репо, ссылки на `docs/`, кросс-репо, деплой); паритет с `lendings-vps-infra/AGENTS.md`.
- **Сводный кросс-репо аудит:** канон — `lendings-vps-infra/docs/audit/cross-repo-portfolio-audit-2026-04-ru.md` (12 направлений, нэйминг, декомпозиция, дедупликация, унификация правил, лучшие практики, roadmap). В этом репо таблицы не дублируются; backlog — в [`audit/audit-and-improvement-plan-ru.md`](audit/audit-and-improvement-plan-ru.md).
- **Чеклист DOC-04:** [`docs/guides/feb-code-post-release-sync-checklist-ru.md`](guides/feb-code-post-release-sync-checklist-ru.md) — сверка с Feb Code после их релизов; ссылка из [`audit/cross-repo-alignment-ru.md`](audit/cross-repo-alignment-ru.md).
- **Инфра DEV-04:** в [`lendings-vps-infra/docs/operations/multisite-vps-deploy-ru.md`](https://github.com/Jackman108/lendings-vps-infra/blob/master/docs/operations/multisite-vps-deploy-ru.md) — таблица контрольного прогона после выкладки (TLS, healthcheck, rollback); контур из этого репозитория — [`guides/deploy-vercel-and-vps-ru.md`](guides/deploy-vercel-and-vps-ru.md).
- **Кросс-репо с Feb Code (C1/C2/форматирование):** [`guides/scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md) — политика ESLint vs Prettier по репозиториям; [`docs/guides/layer-imports-and-public-api-cross-repo-ru.md`](guides/layer-imports-and-public-api-cross-repo-ru.md) — слои/импорты и публичный API на фоне `febcode/docs/guides/architecture-ru.md`. ADR [`0010`](adr/0010-formatting-policy-no-prettier.md) — раздел «Кросс-репозиторий Feb Code».
- **Кросс-репо с Feb Code (C3):** [`guides/scripts-and-quality-gates-ru.md`](guides/scripts-and-quality-gates-ru.md) — принципы тестирования и ссылки на канон Feb Code без дублирования; [`docs/audit/cross-repo-alignment-ru.md`](audit/cross-repo-alignment-ru.md) — строка статуса по C3/C4.
- **Инфраструктура VPS:** выделен отдельный репозиторий **`lendings-vps-infra`** (`docker-compose*.yml`, `nginx/`, `secrets/*.env.example`, `docs/`, `.cursor/rules`); выполнен `git init`. Корневой каталог **`deploy/`** в этом репозитории приложения удалён; compose/nginx на сервере ведутся только в infra-репо; локальные `deploy/secrets/*.env` при необходимости перенести вручную (см. [`guides/deploy-vercel-and-vps-ru.md`](guides/deploy-vercel-and-vps-ru.md)).
- **Документация (deploy):** ранее в корневом **`deploy/`** — материалы перенесены в **`lendings-vps-infra`**; в этом репо канон — [`guides/deploy-vercel-and-vps-ru.md`](guides/deploy-vercel-and-vps-ru.md).
- **Документация:** `docs/README.md` — ссылки на [`guides/deploy-vercel-and-vps-ru.md`](guides/deploy-vercel-and-vps-ru.md) и `audit/cross-repo-alignment-ru.md`; переработан `docs/audit/cross-repo-alignment-ru.md` (эталоны обоих репо, матрица унификации, ссылка на `febcode/docs/guides/cross-repo-alignment-plan-ru.md`, пути после переноса клонов); записи в `docs/README.md` и `docs/audit/README.md`.
- **Юридическое соответствие (РБ Постановление Совмина №31):** в `shared/constants/company.ts`
  расширен `SITE_OPERATOR` до полноценного **единого источника правды** для реквизитов ИП —
  добавлены `legalEntity` (ФИО, УНП, дата/орган регистрации; неподтверждённые поля = `null`,
  UI скрывает строку), `phone`, `publicEmail`, `address.full`, `workHours.{summary,slots}`,
  `bankDetails`. Добавлен компонент `shared/ui/legal-requisites/LegalRequisites.tsx` (+ unit-тесты)
  и подключён в `widgets/layout/ui/footer/Footer.tsx`. Потребители (`Phone`, `WorkHours`, `Address`,
  `generateStructuredData`) переведены на `SITE_OPERATOR` — литералы телефона/адреса/часов из
  компонентов удалены; в JSON-LD добавлены `telephone`, `email`, `legalName`, `openingHoursSpecification`.
- **Multi-site VPS deploy:** исторически корневой каталог **`deploy/`** содержал `docker-compose.yml` (compose v2,
  pull-only, `nginx 1.27-alpine` + Let's Encrypt через `certbot`), мульти-доменная конфигурация
  (`premium-design.pro`/`www`, `febcode.pro`/`www`, ACME `_acme.conf`), HSTS, упреждающий редирект
  www → апекс, иммутабельный кеш `_next/static/`. Образы тянутся из GHCR, исходники второго сайта
  на VPS не клонируются — репозитории и локальные папки остаются изолированными. Операционный
  гайд — [`lendings-vps-infra/docs/operations/multisite-vps-deploy-ru.md`](https://github.com/Jackman108/lendings-vps-infra/blob/master/docs/operations/multisite-vps-deploy-ru.md).
- **Лицензия и интеллектуальная собственность:** в корне репозитория опубликованы [`LICENSE`](../LICENSE)
  (proprietary, EN) и [`LICENSE_RU.md`](../LICENSE_RU.md) (RU, преимущество для законодательства РБ).
  Принят ADR [`0011-proprietary-license.md`](adr/0011-proprietary-license.md). В `package.json`
  поле `license` → `SEE LICENSE IN ../LICENSE`, добавлен `author`.

### Changed

- **Документация (структура):** переработан [`docs/README.md`](README.md) — порядок «гайды → аудит → ADR → операции → MemPalace»; [`audit/README.md`](audit/README.md) — указатель без дублирования таблиц; сокращены [`README.md`](../README.md), [`development-playbook-ru.md`](development-playbook-ru.md), [`audit/cross-repo-alignment-ru.md`](audit/cross-repo-alignment-ru.md) (строка матрицы «Форматирование»), [`audit/project-audit-ru.md`](audit/project-audit-ru.md); [`SCRIPTS`](guides/scripts-and-quality-gates-ru.md) / [`SCRIPTS / C3`](guides/scripts-and-quality-gates-ru.md) — без лишних перекрёстных дублей (как в прошлой итерации).
- **DOC-05:** в [`shared/constants/company/index.ts`](../src/shared/constants/company/index.ts) заполнены **`registrationAuthority`** (Жлобинский районный исполнительный комитет, ЕГР) и **`registrationDate`** (10.06.2019); обновлён unit-тест [`shared/ui/legal-requisites/LegalRequisites.test.tsx`](../src/shared/ui/legal-requisites/LegalRequisites.test.tsx).
- **Закрытие бэклога SEC-04 / DOC-04 / DEV-04:** журнал триажа в [`docs/adr/0008-react-chatbot-kit-dependency.md`](adr/0008-react-chatbot-kit-dependency.md) (`yarn audit` — 0 findings на 2026-04-29); чеклист Feb Code — [`feb-code-post-release-sync-checklist-ru.md`](guides/feb-code-post-release-sync-checklist-ru.md); DEV-04 — таблица в `lendings-vps-infra`. Далее закрыт **DOC-05** (см. выше).
- **Документация и точки входа:** `premier-design/README.md` — требования Node согласованы с **`engines`** и `.nvmrc`, добавлены ссылки на кросс-репо гайды (Feb Code); [`docs/README.md`](README.md) — таблица «один источник правды» (скрипты+engines, кросс-репо); [`docs/development-playbook-ru.md`](development-playbook-ru.md) — строка в карте источников; [`.cursor/rules/agent-quality-process.mdc`](../.cursor/rules/agent-quality-process.mdc) — синхронизация при смене `engines`/`.nvmrc` и матрицы Feb Code.
- **`docs/guides/scripts-and-quality-gates-ru.md`:** разделы про **`engines.node`**, `.nvmrc`, `Dockerfile.prod`, политику форматирования и ссылки на новые гайды.
- **Next.js config (C4, паритет с `febcode/next.config.ts`):** `outputFileTracingRoot`, `turbopack.root`, `webpack.watchOptions` с игнором `node_modules`/`.next`/системных путей Windows и `followSymlinks: false` — см. `premier-design/next.config.js`.
- **`package.json`:** поле `engines.node` (`>=22.19 <25`, совместимо с комментарием в `Dockerfile.prod` про lighthouse; целевой рантайм — `.nvmrc` / образ `node:24`); `typescript` перенесён из `dependencies` в `devDependencies` (сборка в Docker по-прежнему через полный `yarn install`).
- **CLS (Lighthouse):** убраны искусственная задержка `setTimeout(100)` и флаг `isReady` в `useShareBanner`; промо-баннер (`ShareBanner`) переведён на один слот с `aspect-ratio` и `next/image` `fill`, чтобы смена desktop/mobile не меняла высоту ряда; для шрифтов Google — `adjustFontFallback` в `lib/interFont.ts`.
- **CSP:** директива `upgrade-insecure-requests` убрана из `Content-Security-Policy` в `next.config.js` — на HTTP без TLS (локальный стек в **`lendings-vps-infra`**: `docker-compose.dev.yml`, `:8080`) Chrome иначе запрашивал `/_next/*` как `https://` → `ERR_SSL_PROTOCOL_ERROR`. Ужесточение HTTPS остаётся за TLS-прокси (nginx/Vercel) и заголовком `Strict-Transport-Security` при включённом hardened-режиме.
- **Соцсети — один источник:** `SITE_SOCIAL` в `shared/constants/company.ts`; `SocialIcons` и `structuredData.sameAs` читают оттуда (Telegram / VK / Instagram).
- **EstimateModal:** фиксированы `max-height` контейнера, левый блок с коллапсами со скроллом слева, правый с результатом — стабильная зона, без скачков при раскрытии; шапка/подвал модалки `flex-shrink: 0`.
- **Deploy (VPS):** сервис/контейнер Docker для сайта переименованы в `premium-design`, `PREMIUM_DESIGN_IMAGE` в `deploy/.env.example`, `secrets/premium-design.env`, nginx `upstream` → `premium-design:3000` (см. [`guides/deploy-vercel-and-vps-ru.md`](guides/deploy-vercel-and-vps-ru.md)).
- **Домен и реквизиты:** production-origin и nginx — `https://premium-design.pro` (конфиги nginx в **`lendings-vps-infra`**: `nginx/conf.d/premium-design.pro.conf`, dev — `nginx/conf.d/dev/premium-design.pro.dev.conf`). В `SITE_OPERATOR.legalEntity.unp` указан `491455920`. Футер: колонка реквизитов и блок «Разработано» прижаты к низу грида, копирайт с `padding-bottom` в запас плавающей панели, кредит — по центру в правой колонке; на мобилке — стек с разделителями, реквизиты с сеткой подпись/значение.
- **Документация процесса:** `docs/README.md`, `.cursor/rules/agent-mempalace-bootstrap.mdc`, `agent-quality-process.mdc`, `audit/quality-gates-sync-ru.md`, `audit/deploy-readiness-ru.md`, `guides/perf-and-seo-checklist-ru.md` — таблица «один источник правды», описание CSP/HSTS и workflow GHCR приведены к текущей схеме.
- **Docker:** `premier-design/.dockerignore`; образ CI — [`../.github/workflows/ghcr-premium-design.yml`](../.github/workflows/ghcr-premium-design.yml); dev: `http://localhost:8080` отдаёт HTML-лендинг со ссылками на оба сайта (`lendings-vps-infra/nginx/dev-static/localhost.html`, volume в `lendings-vps-infra/docker-compose.dev.yml`).

### Removed

- **PostgreSQL и dev-инфраструктура (не использовалась в коде):** удалены каталоги
  `pgdata/` (`init.sql`, `Dockerfile`), `nginx/` (legacy `nginx.conf.dev`/`prod` со ссылками на
  отсутствующий `/pgdata`), корневые `docker-compose.yaml` и `docker-compose.development.yaml`,
  `premier-design/openssl.cnf`. Из `package.json` удалены неиспользуемые зависимости
  `pg-promise`, `dotenv` (deps), `pg`, `pg-query-stream`, `@types/pg` (devDeps).
  Замена для деплоя — [`guides/deploy-vercel-and-vps-ru.md`](guides/deploy-vercel-and-vps-ru.md) и репозиторий **`lendings-vps-infra`** (см. актуальные записи выше).

### Added (предыдущие)

- **Правила агента и архитектура:** канон норм — [`docs/mempalace/rules/`](mempalace/rules/); в Cursor — `agent-mempalace-bootstrap.mdc` (MemPalace-first, `alwaysApply`) + `agent-quality-process.mdc` (гейты, pre‑commit, RISK‑08, DoD); дублирующие длинные `.mdc` удалены в пользу чтения `mempalace/rules` из репозитория. Проектные паттерны (`UiDialog`/`<dialog>`, `useScrollToElement`, `body` + SVG паттерн, темизация хедера) — в [`02-web-ui-components-and-tokens-ru.md`](mempalace/rules/02-web-ui-components-and-tokens-ru.md) и [`07-web-testing-and-quality-ru.md`](mempalace/rules/07-web-testing-and-quality-ru.md).
- **Скрипт `typecheck`:** `tsc --noEmit --pretty` как отдельная команда; включён в `check:static` (lint → typecheck → unit) и в `ci:quality`. Удалён мёртвый скрипт `type` (ссылался на несуществующий `pages/api/importData.mjs`). Сгруппирован порядок `scripts` в `package.json` (prepare → dev/build → lint/typecheck/test → check:\* → reports → e2e → storybook/panda).
- **Документация и DX:** [development-playbook-ru.md](development-playbook-ru.md) переработан в строгий навигатор (карта чистой архитектуры + ссылки на нормы, без дублирования); [docs/README.md](README.md) — таблица «один источник правды». Открытый бэклог [docs/audit/audit-and-improvement-plan-ru.md](audit/audit-and-improvement-plan-ru.md) переоформлен как **executable compliance‑план** (разделы: DOC/ARCH/QA/CI/PERF/SEC/OBS/UI/DEV; формат ID — приоритет — источник — задача — артефакт — статус; DoD).
- **Документация и DX:** [guides/scripts-and-quality-gates-ru.md](guides/scripts-and-quality-gates-ru.md) и [audit/quality-gates-sync-ru.md](audit/quality-gates-sync-ru.md) синхронизированы с новой группировкой `scripts` и новым `typecheck` (включая упоминание в [deploy-readiness-ru.md](audit/deploy-readiness-ru.md)).

- **Портфолио (`/portfolio`):** секция `PortfolioProjectSliders` — по одному крупному слайдеру на каждый объект из `examplesCard` (заголовок проекта + 16:9, стрелки `keen-slider`); остальные страницы по-прежнему используют `Examples` с карточками.
- **CTA `Appeal` везде в одной подаче:** `appealSection` в `getCommonProps` (SSG) и `Appeal` в `app/documents/*` (`DocumentSiteShell`), в `ServiceCategoryPage` / `ServiceDetail` / `RelatedServiceDetail`. Контракт `AppealSectionData` в `shared/interface/appealSectionData.props.ts`. **Хедер** `headerVariant: 'solidDark'` (тёмный фон + светлое меню) — документы и витринные/детальные страницы сервисного тира без full-screen hero.
- **Страница `/services`:** каталог категорий прайса карточками (`ServiceCategoriesHub`), секция **RelatedServices** (как на главной, заголовок из блока `title` с `shortTitle: "related-services"`), **BusinessServices**, hero и призыв как на «Ремонт»; в `data.json` — пункт меню **Services** / «Услуги», запись `titlesPage` с `shortTitle: "services"`.
- **Баррели / композиция:** `features/category/index.ts` (`@features/category`); расширен `features/services/index.ts` (`ServiceCategoryPage`); **`widgets/services-tier`** — `ServicesTierPage` для `pages/services/[categoryId].ts` (без cross-feature между `features`); **`shared/ui/pricing-table`** — общие стили таблицы прайса для `CategoryCollapse` и `ServiceCategoryPage`.
- **HTML-карта сайта `/sitemap`:** полный перечень URL (те же pathnames, что и в `sitemap.xml` через `lib/collectSitePathnames.ts`); ссылка на XML в тексте страницы; в `titlesPage` — `shortTitle: "sitemap"`.
- **Тесты:** `lib/__tests__/collectSitePathnames.test.ts`; расширен `tests/api/sitemap.handler.test.ts`.

### Fixed

- **TS regression в тестах:** `shared/hooks/__tests__/useVisibilityObserver.test.ts` — нарушение control‑flow narrowing на `let instance`; перенесено в `instanceRef.current` (плавающая ссылка через объект), `yarn typecheck` теперь зелёный.
- **CI / supply-chain:** шаг `yarn audit --level critical` падал с кодом `4` при moderate в транзитивном `postcss` (Yarn v1: ненулевой exit при любой находке в дереве). Добавлена `resolutions.postcss: ^8.5.10` (moderate XSS в stringify, npm advisory [1117015](https://www.npmjs.com/advisories/1117015)), обновлён `yarn.lock` — `yarn audit` даёт **0 vulnerabilities**.

### Changed

- **Cursor / MemPalace-first:** удалены `.cursor/rules/agent-architecture-clean-code.mdc` и `agent-context-bootstrap.mdc`; добавлен `.cursor/rules/agent-mempalace-bootstrap.mdc` (`alwaysApply: true`) — перед работой обязателен Read из `docs/mempalace/rules/` и при включённом MCP — семантический поиск по дворцу с верификацией по файлам в git. Репозиторные merge-инварианты перенесены в конец [`docs/mempalace/rules/01-web-architecture-and-boundaries-ru.md`](mempalace/rules/01-web-architecture-and-boundaries-ru.md). Обновлены `docs/README.md`, `development-playbook-ru.md`, `mempalace/README.md`, `mempalace-agent-memory-ru.md`, `audit/project-audit-ru.md`, `audit/audit-and-improvement-plan-ru.md` (DOC-01).
- **S1 audit sprint (QA-01 / DOC-01 / CI-02 / SEC-02 / OBS-01):** в `ci.yml` добавлен отдельный шаг `Typecheck`; weekly `security-high-weekly.yml` дополняет issue ссылкой на workflow run и artifact `security-high-weekly`; форма обратной связи защищена от двойной отправки (`isSubmitting` guard + disabled submit + unit test); runbook observability дополнен RCA drill для `correlationId`; `audit-and-improvement-plan-ru.md` обновлён статусами `Done` по задачам S1.
- **S2 audit sprint (ARCH-01 / ARCH-02 / DOC-02 / CI-01 / UI-01):** подтверждён нулевой архитектурный долг allowlist (`entries=[]`, `maxAllowedCount=0`) и зелёные `check:architecture*`; зафиксирован аудит use-case границ для feedback/quiz (без HTTP/`fetch` в use-case); уточнено покрытие `lint-staged` по слоям; документирован SLA-бюджет CI (`p95 <= 25`) и источник проверки в `ci-trends.yml`; проведён аудит dark-theme контрастов на медиа-страницах без необходимости токен-правок.
- **S3 audit sprint (QA-02 / QA-03 / SEC-01 / OBS-02 / DOC-03):** завершён аудит `jest.mock` на полноту контрактов хуков (включая `scrollToRef`), добавлена Zod-валидация `FEEDBACK_*` env в `submitFeedback` с безопасным fallback и негативным тестом, подтверждены CSP-политики по ADR `0004` без необходимости изменения `next.config.js`, зафиксирован runbook по ретраям/таймаутам SMTP+Telegram (RISK-10) и добавлен unit-test на SMTP transient retry.
- **S4 audit sprint (PERF-01 / PERF-02 / UI-02 / DEV-01 / DEV-03):** выполнен perf-прогон `check:perf:ci` (initial JS в бюджете, Lighthouse summary сохранён), зафиксирован аудит реестра `dynamicSectionImports` без расхождений, для legacy-модалок (`FeedbackModal`, `EstimateModal`) добавлен fallback закрытия по `Escape` и unit-покрытие (PhotoViewer уже покрыт в e2e smoke), в `premier-design/README.md` добавлен блок «Первый день», создан `CONTRIBUTING.md` с коротким регламентом contribution и ссылками на правила/плейбук.
- **Backlog closure (ARCH-03 / QA-04 / PERF-03 / SEC-03 / DEV-02):** в `check-architecture-boundaries.mjs` добавлен явный запрет `@lib/find*` в `features/*` и sentinel-test; для primitives Storybook добавлены play/a11y-проверки (`UiDialog`, `UiInput`) и подтверждена сборка `build-storybook`; обновлён ADR `0008` и добавлен RFC `rfc-chatbot-runtime-isolation-ru.md`; weekly security triage усилен обязательной triage-матрицей в issue template и синхронизацией `supply-chain-ru.md`; принято ADR `0010` (без внедрения Prettier в текущем цикле).
- **Noise-gate DX:** добавлен скрипт `clean:test-artifacts` и интегрирован в начало `check:risk:local`, чтобы **`ci:quality`** автоматически очищал `test-results`/`playwright-report`/`blob-report` перед `check:noise`.
- **GitHub Actions:** `actions/checkout@v6`, `actions/setup-node@v6`, `actions/upload-artifact@v7` в `ci.yml`, `ci-trends.yml`, `e2e-extended.yml`, `security-high-weekly.yml`, `deploy.yml` — актуальный runtime JS-actions (меньше шума deprecation Node 20 у checkout/setup-node/upload).
- **Смета (оценка стоимости), нейминг:** модуль `shared/ui/estimate-modal` (`EstimateModal`, `EstimateButton`, `useEstimateModalHandlers`, `public/panel/estimate.svg`, `panelData.id: estimateButton`, `offerBanner.estimateType`); публичные копии и `shortTitle` — про смету/оценку, без отдельного маркетингового URL. **ThemeButton:** иконка `2.2rem` (`1.95rem` ≤768px) — визуально крупнее блока соцсетей (`1.8rem`). **Phone:** иконка трубки и номер выровнены по вертикали.
- **Хедер:** `WorkHours` и блок телефона — одинаковые `min-height: 44px` и `box-sizing: border-box`; `ThemeButton` — крупная иконка (см. выше) и flex-центрирование.
- **Карточки / контраст / фокус (доп. унификация):** карточки услуг, примеров, сметы и прайс-таблица приведены к единому паттерну `border: 1px solid var(--color-border)` + `var(--shadow-md/--shadow-lg)` без смешения с inset/pseudo-3D; для фото-плашек введены семантические токены `--surface-on-media*` и `--text-on-media*` (используются в `OfferBanner`, `BusinessServicesCard`, `ServicesCards`); для клавиатурной навигации добавлены `--focus-outline-*` и глобальный fallback `:focus-visible`, точечно закрыты пропуски (`HomePageChrome`, `ChatBotSidebar`).
- **Закрытие хвоста UI-аудита:** формы получили `autofill`-состояния в `UiInput`/`UiTextarea` + `autocomplete="off"` для свободного поля сообщения; добавлен `e2e/visual-regression.spec.ts` и команда `yarn test:e2e:visual` для CI-smoke по консистентности карточек и контрасту dark-overlay; `PROJECT_AUDIT` и `AUDIT_AND_IMPROVEMENT_PLAN` синхронизированы.
- **Отдельный маршрут витринной сметы:** снят ранее; меню, `titlesPage` и sitemap обновлены. Модалка сметы на месте: `EstimateButton`, `Costing`, `EstimateModal`, `offerBanner.estimateType`.
- **Главная / внутренние:** CTA `Appeal` вынесен из `HomePageSection` (как на `repairs` / `design`) с `sectionId` / `aria-label` для `home-appeal`. **Блок `Features` после `HeroBanner`:** как на главной — внешние отступы секции = 0, `z-index`/stack, к карточке визуальное смыкание с низом hero (`features__container`); **хедер `solidDark`:** `--color-header-bar-bg` / `--color-header-bar-border` (brown/black), не `var(--blue)`.
- **Главная — квиз и навигация:** секция `LeadQuiz` с `id="lead-quiz"` (scroll-spy / боковое меню); в `data.json` и `dataPropsSchema` — `homePage.sectionAriaLabels.quiz` (`"Квиз"`). **Код-гигиена (TS/TSX, не стили):** `import type` и алиас `@lib/getStaticData` в `app/documents/layout`; выравнивание импортов в `Header`, `AppealBanner`, `ServiceCategoryPage` и др.
- **Ритм секций (главная + остальные страницы):** в `styles/tokens.css` — `--section-padding-y*`, **`--section-content-max`** (как `min(90rem, 100%)` у детей `HomePageSection`), **`--section-inner-stack-gap`**, **`--section-card-padding-block`**, **`--page-content-offset-top`** (шапка/документы/sitemap). Внутренние контейнеры карточек/гридов (вместо `1440px`/`4vh`/`2vh`) и футер/хедер/cookies приведены к этим токенам; `HomePageSection` + `data-rhythm` + сброс вложенных отступов на главной — без изменений логики.
- **HomePageChrome:** боковая панель — **ширина по содержимому** (`width: max-content`), верхняя граница `min(100vw - 1.25rem, 90vw)`, внутренний зазор `var(--space-sm)`; scroll-spy — `HOME_SECTION_SCROLL_SPY_ORDER` (пункт «Преимущества» в меню убран).
- **Этап 1 (визуальный аудит):** token-driven spacing и типографика доведены для всех целевых страниц (`home/services/repairs/documents/about/contacts/design/portfolio`): расширена шкала `--space-xl/--space-2xl`, секционные контейнеры и отступы приведены к токенам (`HomePageSection`, `Examples`, `Costing`, `Partners`, `aboutNews`, `Address`, `Appeal`, `Offer`, `ContactsMicroUspAside`), выровнены заголовки/описания (`Title`, `CompanyAboutSections`, `OfferBanner`), ограничена длина строки длинного текста (`pageDetailShell`, `DocumentPage`, `OfferBanner`, `Partners`, `aboutNews`).
- **Этап 2 (a11y):** min target `>=44px` и `@media (hover: hover)` для hover распространены на остальные интерактивы (модалки, смета в модалке, футер/хедер вспомогательные, карточки, таблица прайса, новости, соц-иконки, стрелки, hero-кнопка CTA, cookies и др.); базовый слой из предыдущей итерации (`HeaderMenu`, `HomePageChrome`, `OrderButton`, …) сохранён; `lib/dynamicSectionImports.ts` с `SectionSkeleton` без изменений по этому PR.
- **OfferBanner:** усилен контраст текста на фоне (более плотный scrim/overlay, контрастная карточка контента, белый текст с тенями, повышенная читаемость списков/описаний).
- **SSG услуг:** из **`staticPathsHandler`** и **`staticPropsHandler`** удалён неиспользуемый режим **`isRelated`** (маршрут **`[categoryId]`** обслуживает **`servicesTierStatic`**); добавлены тесты **`lib/__tests__/servicesTierStatic.test.ts`**.
- **RelatedServiceDetail:** из **`RelatedServiceDetailProps`** и пропсов SSG (`servicesTierStatic`) убрано неиспользуемое поле **`titles`**.
- **Документы (`app/documents/*`):** хлебные крошки перенесены **внутрь** `document__container` (не под absolute-header); компактные стили `DocumentBreadcrumbs.module.css`; верхний отступ shell — **`var(--page-content-offset-top)`** (как у `html` `scroll-padding-top`). Детальные услуги: то же в `pageDetailShell.module.css`.
- **Шапка:** в `HeaderMenu.module.css` увеличен `max-width` контейнера ссылок для большего числа пунктов.
- **Sitemap API:** формирование URL через `collectSitePathnames` и `STATIC_SITEMAP_PATHS` (включая `/services`, `/sitemap`); дублирования логики нет.
- **`public/robots.txt`:** комментарий про HTML `/sitemap`.
- **Данные сайта:** тип **`DataProps`** = **`z.infer<typeof dataPropsSchema>`** в `shared/validates/dataPropsSchema.ts` (детализированы блоки прайса, related, меню, новости, офферы, партнёры, отзывы, карточки секций и др.); `getData()` возвращает результат `safeParse` без `as unknown as`. `widgets/interface/interfaceData.ts` — реэкспорт `DataProps`/`GetDataProps` и алиасы полей; `features/category/interface/Category.props.ts`: **`Prices`** = `DataProps['prices']`.
- **Публичный API фич:** корневые `features/*/index.ts` (banner, news, papers, contacts, company-about, partners, related-services, services, buttons-panel, coasting, documents-content) и импорты **`@features/<slice>`** из `pages/`, `widgets/`, `app/documents/*` вместо глубоких путей к `ui/`.
- **FSD (границы слоёв):** контракты SEO в `shared/interface/seoHead.props.ts`, canonical — `shared/utils/getFullCanonicalUrl.ts`. `getCommonProps`, `findService`, `findRelatedService` в `lib/` с типом `DataProps` из схемы (см. выше). `useViewportMobile` в `shared`, `useResizeEffects` в `widgets/layout/hooks/`. Удалены старые finders в `features/*/utils`; обновлены `docs/audit/project-audit-ru.md`, `docs/guides/code-structure-and-naming-ru.md`.
- **FSD / тесты хуков:** удалён пустой корень `hooks/` (остались только перенесённые файлы); unit-тесты колокейтнуты в `shared/hooks/__tests__/`, `shared/ui/order/hooks/__tests__/`, `features/*/hooks/__tests__/`, `widgets/*/hooks/__tests__/`. Убраны алиас `@hooks/*` и строка `hooks/**/*` из `collectCoverageFrom` в Jest. Доки: `docs/guides/code-structure-and-naming-ru.md`, `docs/mempalace/rules/07-web-testing-and-quality-ru.md`.
- **Футер / подрядчик:** блок «Разработано» вынесен в `widgets/layout/ui/footer/DeveloperCredit/`; URL и ассет — `shared/constants/company.ts` (`DEVELOPER_STUDIO_FEB_CODE`). Зафиксировано в `docs/guides/code-structure-and-naming-ru.md`.
- **Оператор сайта (единый модуль):** `shared/constants/company.ts` — `SITE_OPERATOR` (домен, бренд, копирайт, контакт для ПДн, юр. лицо, данные для JSON-LD); потребители: `getFullCanonicalUrl`, sitemap API, `generateStructuredData`, `CustomHead`, `Copyrighting`, юридические тексты в `features/documents-content`. `check-noise-artifacts`: нарушение только если `.next/` попал в **git** (локальный кеш после `yarn dev` не блокирует `check:risk:local`).
- **Документация и конфиг:** убраны неиспользуемые алиасы `@entities/*` и `@config/*` из `tsconfig.paths.json` и `jest.config.cjs` (соответствующих каталогов в приложении нет). `README.md` — описание SMTP приведено к фактическому поведению (`EMAIL_HOST` + полный набор в любой среде). `docs/guides/code-structure-and-naming-ru.md` — явно зафиксирована роль `lib/` и `dynamicSectionImports.ts`.
- **Аудит (апрель 2026):** сводный документ `docs/audit/project-audit-ru.md` + восстановлен `docs/audit/audit-and-improvement-plan-ru.md` (открытый бэклог, DoD). Закрыты пункты аудита §«Итоги»: (1) реестр `next/dynamic` перенесён из `shared/utils/dynamicImports.ts` в `lib/dynamicSectionImports.ts` (`@lib/dynamicSectionImports` в `pages/*`); `scripts/check-architecture-boundaries.mjs` учитывает динамический `import('@features/…')` / `@services/…`. (2) `shared/validates/dataPropsSchema.ts` + валидация в `lib/getStaticData.ts`, тесты `dataPropsSchema.test.ts`. (3) удалены неиспользуемые `services/processFeedback.ts` и тест. Индексы `docs/README.md`, `docs/audit/README.md`, `project-risk-register-2026-04-ru.md` синхронизированы.
- **Телефон (валидация):** новый модуль `shared/validates/byRuPhone.ts` и константы `PHONE_FORMAT_ERROR`/`PHONE_BY_RU_OPERATOR_ERROR` в `shared/validates/feedbackSchema.ts`; допустимы BY +375 с операторами `25/29/33/44` и RU +7 с мобильным планом `9XX` (900–953, 955–969, 972–999; `954/970/971` запрещены). UI-сообщение про коды в `useFeedbackForm`. `shared/ui/order/utils/validatePhone.ts` делегирует в общий модуль (единый источник истины); тесты: `shared/validates/__tests__/byRuPhone.test.ts` и расширенный `features/feedback/__tests__/schema.test.ts`.
- **Feedback use-case:** отправка SMTP и Telegram теперь параллельна (`Promise.all`) при полном SMTP — wall-clock ≈ max(branch), а не сумма; снижает 504 по `FEEDBACK_API_TIMEOUT_MS` в dev. Письмо включается при `EMAIL_HOST` + полный набор `EMAIL_*`+`FEEDBACK_EMAIL_TO` (в любой среде); без `EMAIL_HOST` — только Telegram; при `EMAIL_HOST`, но неполном SMTP — `console.warn`, а не `500`. `feedbackDal` передаёт в `emailService.sendMail` сокращённые `connection`/`socket` таймауты (5/5/6s); `telegramService` использует `TELEGRAM_REQUEST_TIMEOUT_MS` (по умолчанию 8s, ранее 10s у axios). Таймаут маршрута `POST /api/feedback` по умолчанию: dev **30s**, prod **20s** (`FEEDBACK_API_TIMEOUT_MS` перекрывает). Индекс обновлён в `.env.example`.

### Changed (historical)

- **DRY (страницы + данные):** вынесены `selectAppealSectionData` и `APPEAL_SECTION_PAGE_KEYS` в `shared/hooks/usePageData.ts`; `mapFaqEntriesToStructuredData` в `shared/utils/faqStructuredData.ts`; удалены пустые дубликаты-заглушки (`features/home`, `widgets/contacts-page`, `widgets/home-page` и пр.) и пустые каталоги под `features/` и `widgets/`.
- **Контакты (секция USP):** `ContactsMicroUspAside` перенесён в фичу `features/contacts/ui/ContactsMicroUspAside/` (ранее `widgets/contacts-micro-usp-aside/`), проп-тип — `features/contacts/interface/ContactsMicroUspAside.props.ts`.
- **Форма обратной связи:** `FeedbackModal` + `FeedbackForm` на семантических токенах, крупная модалка (до `~48rem`), градиент/тень, сетка «имя + email» от `28rem` ширины, компактные поля **без внутреннего скролла**; `UiTextarea` в режиме `textareaTight` (фиксированная высота, `resize: none`). Письмо: при `EMAIL_HOST` и полном SMTP-наборе (любая среда); без `EMAIL_HOST` — только Telegram. Неполный SMTP при заданном `EMAIL_HOST` — `console.warn`, не 500. Telegram — по умолчанию. Таймаут маршрута `POST /api/feedback` по умолчанию: **30s** в development, **20s** в остальных средах (`FEEDBACK_API_TIMEOUT_MS` по-прежнему перекрывает), чтобы реже ловить **504** на retry+Telegram/SMTP при старом лимите 8s.

- **Новости / Layout:** отключение дубля hash-sync в футере на `/about` через **`footerNewsHashSyncOnMount={false}`** в `pages/about.tsx` и проп **`newsHashSyncOnMount`** у `Footer` (без `useRouter` в футере — совместимость с `app/documents/*` и проход `yarn build`).

- **PhotoViewer (примеры работ):** контейнер кадра **90vw × 90dvh** (90% ширины и высоты вьюпорта), внутри `object-fit: contain`; `next/image` — `sizes="90vw"`; текст и кнопки поверх scrim — **`var(--white)`** + лёгкая тень; **a11y:** скрытые заголовок/описание и `aria-labelledby` / `aria-describedby` (`useId`). **`jest.setup.ts`:** полифилл `<dialog>` только при наличии `HTMLDialogElement` (не ломает тесты в среде `node`). Документация: ADR `docs/adr/0003-modal-standard-and-adapters.md`, `docs/mempalace/rules/02-web-ui-components-and-tokens-ru.md`, `07-web-testing-and-quality-ru.md`.

- **Документация и правила (апрель 2026):** обновлены `.cursor/rules/agent-architecture-clean-code.mdc` (темизация, контролируемые модалки, `useBackgroundLoader`, моки хуков с полным return), `agent-context-bootstrap.mdc` (проверка тёмной темы); ADR `docs/adr/0003-modal-standard-and-adapters.md` — раздел с паттерном новостей; `docs/mempalace/rules/02-web-ui-components-and-tokens-ru.md` (тёмная тема, scrim, таблицы); `07-web-testing-and-quality-ru.md` (моки `useScrollToElement` + `scrollToRef`); индекс `docs/README.md` со ссылками на mempalace-гайды.

- **Риски RISK-06, 08, 09, 11, 12 (док+код):** `postFeedbackToApi` извлекает `correlationId` из тела/заголовка, `PostFeedbackError` в `useFeedback` (текст ошибки + GTM: `status`, `correlationId`); `405` на `/api/feedback` с `createApiErrorPayload`. Runbook: `docs/audit/operations-observability-ru.md`, `quality-gates-sync-ru.md`, `supply-chain-ru.md`, `ci-cost-and-trends-ru.md`; реестр рисков — все перечисленные ID в статусе **Снижен**; индексы `docs/README.md`, `docs/audit/README.md`.

- **Следующий этап снижения рисков CI (апрель 2026):**
  `@extended e2e` вынесен в отдельный non-blocking workflow `.github/workflows/e2e-extended.yml` (nightly + manual dispatch), merge-gate оставлен на `@core`.
  Добавлен rolling-репорт трендов CI/flake за 14 дней: `.github/workflows/ci-trends.yml` + `scripts/report-ci-trends.mjs` (артефакт `.ci-trends-14d.{md,json}` и summary в job).
  В трендах длительности используется `run_started_at` (без шума очереди раннера) для корректной p50/p95 аналитики.
  Добавлен контроль SLA CI: `scripts/check-ci-sla.mjs` + шаг `Enforce CI SLA budget (p95)` в `.github/workflows/ci-trends.yml` (бюджет p95 для `CI Quality Gates`).
  Архитектурный allowlist вынесен в управляемый реестр `scripts/architecture-allowlist.json` с `maxAllowedCount`; `check-architecture-boundaries.mjs` читает реестр и блокирует рост технического долга.
  Формализован план сокращения allowlist: `scripts/architecture-allowlist-roadmap.json` + `scripts/check-architecture-allowlist-progress.mjs` (минимум -1 по `maxAllowedCount` за итерацию).
  Добавлен `report:architecture-allowlist` (`scripts/report-architecture-allowlist.mjs`) с разбивкой по owner, near-expiry и candidate-to-remove; baseline allowlist сокращен с 25 до 23.
- **Снижение флейковости CI:** `yarn test:e2e` теперь запускает только `@core` smoke-набор; полный прогон доступен через `yarn test:e2e:full`, расширенный — `yarn test:e2e:extended`.
- **Реестр рисков:** расширен анализ эксплуатационных рисков (security supply-chain, внешние интеграции, observability, CI cost/SLA) и добавлен план митигации на май 2026 в `docs/audit/project-risk-register-2026-04-ru.md`.
- **Observability (RISK-11, этап 1):** `/api/feedback` теперь возвращает и логирует `correlationId`; добавлен структурированный server-log envelope (`route`, `method`, `statusCode`, `durationMs`, `timedOut`) для ускорения RCA по 5xx/timeout.
- **Observability (RISK-11, этап 2):** добавлен общий API runtime `shared/lib/api/apiRequestRuntime.ts`; единый envelope `correlationId`/structured logs подключён в `/api/feedback` и `/api/sitemap`.
- **Feedback API refactor:** в `pages/api/feedback.ts` ветвление метода/результата переведено на `switch` без изменения контрактов ответов и SLO-gate поведения.
- **Reliability (RISK-10):** в `submitFeedback` добавлен ограниченный retry-policy для транзиентных ошибок внешних интеграций SMTP/Telegram (`shared/lib/retryAsync.ts`), покрыт unit-тестами; дополнительно in-process **circuit** для SMTP (dev) и Telegram (`shared/lib/integrationCircuit.ts`), fast-fail с HTTP 503, переменные `FEEDBACK_CIRCUIT_*`; детали в `docs/audit/project-risk-register-2026-04-ru.md`. В `premier-design/.env.example` — закомментированные `FEEDBACK_CIRCUIT_*`, `FEEDBACK_API_TIMEOUT_MS`, `FEEDBACK_SLO_ENABLED` для прод-инженеров. Для **504 по wall-clock** (не по логике Telegram): `feedbackDal` передаёт в `emailService.sendMail` сокращённые `connection`/`socket` таймауты; `telegramService` читает `TELEGRAM_REQUEST_TIMEOUT_MS` (по умолчанию 8000ms, ранее 10000ms у axios), чтобы цепочка `retry×2`+SMTP в dev чаще завершалась **до** срабатывания `withTimeout` на `POST /api/feedback`. **Дополнительно:** при полном SMTP доставка письма и Telegram запускаются **параллельно** (`Promise.all`), чтобы wall-clock не складывал две ветки с `retry` подряд.
- **RISK-07 (шум в VCS):** в корневом `.gitignore` игнорируется весь `premier-design/.next/`, а не только `dev/cache` (build output не в коммит); моки `next/link`/`next/image` в `ShareBanner.test.tsx` без утечки `priority` на DOM.
- **Architecture debt reduction (RISK-03):** удалён legacy cross-feature импорт CSS из `RelatedServiceDetail` в `ServiceDetail`; добавлен локальный `RelatedServiceDetail.module.css`; `maxAllowedCount` в allowlist уменьшен до `22`.
- **Architecture debt reduction (RISK-03):** удалена связка `shared/hooks/useFallback.tsx -> @features/services/ui/ServiceDetail/ServiceDetail.module.css`; fallback-стили вынесены в `shared/hooks/useFallback.module.css`; `maxAllowedCount` уменьшен до `21`.
- **Architecture debt reduction (RISK-03):** контракт `CostingCardProps` вынесен в `shared/interface/CostingCard.props.ts`, shared-модули сметы больше не импортируют `@features/coasting/interface/Costing.props`; `maxAllowedCount` уменьшен до `18`.
- **Architecture debt reduction (RISK-03):** `features/buttons-panel/interface/EstimateButton.props.ts` переведён на `@shared/interface/CostingCard.props`; снято ещё одно legacy cross-feature исключение; `maxAllowedCount` уменьшен до `17`.
- **Architecture debt reduction (RISK-03):** `PanelProps` вынесен в `@shared/interface/Panel.props`; сняты исключения для `shared/ui/order/interface/OrderButton.props.ts`, `features/services/interface/ServiceDetail.props.ts`, `features/related-services/interface/RelatedService.props.ts`; `maxAllowedCount` уменьшен до `14`.
- **Architecture debt reduction (RISK-03):** `PanelButton` вынесен в `shared/ui/panel-button`, feature-компонент оставлен как re-export для совместимости; `OrderButton` теперь использует shared-реализацию напрямую, снято ещё одно legacy-исключение; `maxAllowedCount` уменьшен до `13`.
- **Architecture (RISK-03, финал allowlist):** `scripts/architecture-allowlist.json` — `0` legacy-записей, `maxAllowedCount: 0`. Общие DTO (`NewsItem`, `PaperItem`, `Category`/`PriceItem`, `BannerImage`, `ShareBannerData`) в `shared/interface/`; re-export в feature `interface` без cross-import в `ServiceDetail`/`RelatedService`. SSG: `staticPropsHandler` в `lib/staticPropsHandler.ts`, тест `lib/__tests__/staticPropsHandler.test.ts` (старый `shared/utils/staticPropsHandler` удалён). `architecture-allowlist-roadmap.json` — новая политика вместо пошаговых вех 22→15.
- **Cross-device stability:** добавлен `.gitattributes` с фиксацией LF для `premier-design/next-env.d.ts` для исключения EOL-диффов между ОС.
- **Security weekly:** добавлены `.github/ISSUE_TEMPLATE/security_high_weekly.yml`, workflow `.github/workflows/security-high-weekly.yml` и отчёт `scripts/report-yarn-audit-high.mjs` для регулярного triage high/critical уязвимостей.
- **API SLO:** `/api/feedback` теперь пишет telemetry-сэмплы (`shared/lib/feedbackSlo.ts`) и обрабатывает timeout-gate; добавлен проверочный скрипт `scripts/check-feedback-slo.mjs` с порогами p95/error/timeout.
- **Git hooks / precommit:** усилены локальные гейты — `.husky/pre-commit` запускает полный контур качества; `.husky/pre-push` запускает `yarn test:e2e`; добавлен набор скриптов `check:risk:local`, `ci:quality`, `check:architecture:progress`, `check:ci-sla`, `check:slo:feedback`.
- **Playwright/Smoke:** CI-конфигурация переведена на production webServer (`yarn start`) с сериализацией и retry; в smoke-тестах добавлен `gotoWithRetry` и смягчены хрупкие проверки модалки.
- **Perf/Noise gates:** `check-lighthouse-budget.mjs` получил retry и cleanup временных каталогов; `check-noise-artifacts.mjs` и `.gitignore` синхронизированы под `.lighthouse-tmp` / `.lighthouse-chrome-profile`.
- **Lighthouse CI hardening:** `check-lighthouse-budget.mjs` запускает новый Chrome-процесс на каждую попытку, выполняет warmup URL до аудита и использует более стабильные headless-флаги (снижение `net::ERR_ABORTED` в Linux CI).
- **GitHub Actions runtime:** во workflow добавлен `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` для перехода с Node 20 runtime JS-actions и устранения предупреждений deprecation.
- **Риск-менеджмент:** добавлен реестр рисков [`docs/audit/project-risk-register-2026-04-ru.md`](audit/project-risk-register-2026-04-ru.md), обновлены индексы `docs/README.md` и `docs/audit/README.md`.
- **Страница `/portfolio`:** как у `/design` и `/repairs` — сверху `HeroBanner` (отступ под хедер через блок героя), затем `Features`, `OfferBanner`, далее галерея примеров или блок сметы и `Appeal`; баннеры героя — `design_banner` / `repair_banner`.
- **PhotoViewer (базовая вёрстка):** слои `overlayScrim` + `contentRoot`; `next/image` без конфликта с `max-*`; см. актуальный блок **PhotoViewer (примеры работ)** выше — `showModal`, 90vw×90dvh, a11y.
- **Деплой (док + автоматизация):** `yarn ci:quality`; расширен `.env.example` (HTTPS/Vercel, аналитика); e2e — `/portfolio`, просмотр галереи с главной (Escape); таблица в [`docs/audit/deploy-readiness-ru.md`](audit/deploy-readiness-ru.md).
- **Документация:** перестроен [`docs/README.md`](README.md) (маршрут чтения, таблицы по темам без дублирования ADR); индекс [`docs/audit/README.md`](audit/README.md); финальный аудит под деплой [`docs/audit/deploy-readiness-ru.md`](audit/deploy-readiness-ru.md); ссылки в `audit-and-improvement-plan-ru.md`.
- **Шапка (десктоп):** контейнер хедера на flex + перенос навигации на отдельную строку при длинном меню; пункты меню — flex-wrap и адаптивные отступы; высота плейсхолдера при sticky через `ResizeObserver` (`useHeaderPlaceholderHeight`).
- **OfferBanner:** текст и акценты на семантических токенах (`--color-text`, `--color-text-muted`, `--color-accent`) — читаемость в тёмной теме; лёгкая тень заголовка в `.dark`.
- **Бандл (§6.1):** `FeedbackModal` + форма обратной связи подгружаются через `next/dynamic` в `OrderButton` при открытии модалки (`ssr: false`), чтобы вынести тяжёлые зависимости из initial JS общих страниц; для `yarn analyze` по умолчанию не открывается браузер — при необходимости задать `ANALYZE_OPEN=true`.
- **Аудит §3.1, §3.5:** маршрут `/portfolio` (`pages/portfolio.tsx`), меню и `titlesPage` в `data/data.json`, sitemap и тест API.
- **Аудит §3.6, §7.2, §7.3:** ADR [`0007-next-pages-and-app-router-split.md`](adr/0007-next-pages-and-app-router-split.md), [`0008-react-chatbot-kit-dependency.md`](adr/0008-react-chatbot-kit-dependency.md), [`0009-web-analytics-deferred.md`](adr/0009-web-analytics-deferred.md); оглавление [`docs/adr/README.md`](adr/README.md).
- **Аудит §6.2–6.4, §6.6:** чат-кит не импортируется из `useChatBotSidebar`; `ChatBotRuntime` подставляет `initialMessages`; `preconnect` для карт в `pages/_app.tsx`; размеры у `ArrowButton` (`next/image`); `pageExtensions` без `md`/`mdx` в `next.config.js`.
- **Крошки документов:** маршруты и заголовки вынесены в `@features/documents-content/utils/documentBreadcrumbRoutes` (UI только импортирует конфиг).
- **Панель быстрых действий:** док по центру, `z-index: var(--z-dropdown)` ниже баннера cookies (`--z-modal`); отступ `bottom` выше полосы cookies; `FeedbackModal` и `EstimateModal` рендерятся через `BodyPortal` в `document.body`; чат — панель над кнопкой (`position: absolute` от `.chatRoot`); внешний контейнер без `transform`; подпись `PanelButton` — всплытие вверх.
- **Слой представления / хуки:** `useHomePageChrome` → `widgets/home-page-chrome/hooks/`; `useDocumentBreadcrumbs` → `features/documents-content/hooks/` (добавлена обязательная папка `hooks` у фичи); `useCookiesBannerWidget` — композиция модалки cookies вне `ui/CookiesBanner.tsx`.
- **Главная (навигация по секциям):** полупрозрачный «стеклянный» блок и прогресс; активная секция по скроллу (`computeActiveHomeSectionId`, `aria-current="location"`).
- **Документация аудита:** файлы `docs/audit/FULL_project-audit-ru.md` и `docs/audit/audit-and-improvement-plan-ru.md` объединены в один [`docs/audit/audit-and-improvement-plan-ru.md`](audit/audit-and-improvement-plan-ru.md); из таблиц бэклога удалены выполненные пункты; индекс [`docs/README.md`](README.md) обновлён под единую ссылку.
- **Главная:** секции в `pages/index.tsx` + `HomePage.module.css` выровнены по центру (`display: grid`, `justify-items: center`, общий `max-width` для контента) вместо асимметричных отступов `data-align`.
- **Секция «Наш подход»:** в `ApproachCards.module.css` исправлена адаптивность — сетка `1 / 2 / 4` колонки по breakpoints, убраны `margin: 0 30vw` и карточки на фиксированных `vw`, типографика на `clamp` + токены.
- **Аудит §1 (дизайн):** `app/documents/layout.tsx` + `app/documents/DocumentSiteShell.tsx` + `DocumentBreadcrumbs` — юридические страницы в общем `Layout` с крошками; главная — `HomePageChrome` (progress на мобильных, якорное меню на десктопе), `lib/homeSectionNavConfig.ts`, `id`/`aria-label` на секциях, `scroll-padding-top` на `html`; Storybook — `LeadQuiz.stories.tsx`, `TrustSignals.stories.tsx`, glob `features/marketing/**/*.stories` в `.storybook/main.ts`; уточнены токены заголовка/текста в `DocumentPage.module.css`.
- **Шапка (десктоп):** в `HeaderMenu.module.css`, `Menu.tsx` и `Header.tsx` улучшена читаемость пунктов меню — `var(--color-text)` на «липкой» шапке и режим `headerOnHero` (светлый текст + тень) на прозрачном хедере поверх hero.

### Removed

- Секция **«до/после»** (`BeforeAfterShowcase`) с главной страницы; удалён модуль `features/marketing/before-after/`, экспорт из `shared/utils/dynamicImports.ts` и связанные реэкспорты; пункты allowlist в `scripts/check-architecture-boundaries.mjs`.

### Fixed

- **Новости / `TextViewer`:** `onOpenChange(false)`: `flushSync(onClose)` + **`setTimeout(0, () => focus #news-list`)** (после `hideOthers` / размонтирования портала, иначе `aria-hidden` на `main` или кнопка в диалоге); `setExpandedNews` — отдельный `setTimeout(0)` в `useNews`; `DialogClose`; `UiDialog` — `onCloseAutoFocus: preventDefault`.
- **Примеры на главной:** `ExampleCard` — `next/image` для фона с `style={{ width: "100%", height: "auto" }}`, `sizes`, CSS `height: auto` + `object-fit: cover` (предупреждение next/image).
- **Детальные страницы услуг:** отступ под хедером на `min-width: 769px`.
- **Контент и SEO (апрель 2026):** дефолтные буллеты hero — `features/banner/hero/constants/defaultHeroHighlights.ts`; даты новостей 2025–2026; грамматика `stepsWork`; `benefits` для материалов; дубль в `designType` «Оптимальный»; контраст `title-black` / `description-black` в тёмной теме (`Title.module.css`); JSON-LD `Service` + `structuredDataRating` в common props для страниц услуг; `Sitemap` и `BASE_URL` sitemap на `premium-design.pro`; исправлены Storybook-импорты в `TrustSignals.stories.tsx` и мок `CustomHead.test`.
- **Аудит §5.5–5.6, §5.8:** ADR [`docs/adr/0005-rate-limiting-storage-and-client-ip.md`](adr/0005-rate-limiting-storage-and-client-ip.md), [`docs/adr/0006-next-api-cors-same-origin.md`](adr/0006-next-api-cors-same-origin.md); `shared/lib/getClientIpForRateLimit.ts`, env `RATE_LIMIT_TRUST_FORWARDED_FOR`.
- **Аудит §2.1–2.4, §2.6–2.8, §3.2–3.4, §7.4:** контент и блоки в `data/data.json`, `HeroBanner`/`TrustSignals`/`pages/index.tsx` (Reviews до квиза, FAQ, якорь `home-faq`), `features/faq`, `features/company-about`, `features/marketing/video-spotlight`, контакты, карточки портфолио; JSON-LD `@graph` в `generateStructuredData.ts` + пропсы `CustomHead`; тесты `getClientIpForRateLimit`, `generateStructuredData`, реестр ленивых секций (сейчас `lib/dynamicSectionImports.ts`).
- **Аудит §4.1–4.5:** клиент отправки формы — `shared/lib/postFeedbackClient.ts`; SMTP `from`/`to` из `FEEDBACK_EMAIL_*`, `replyTo` с email клиента в `services/dal/feedbackDal.ts`; `/api/sitemap` только `GET` (405 иначе), статические пути через zod, JSDoc; тест 405 в `tests/api/sitemap.handler.test.ts`.
- **CI:** `yarn check:feature-structure` — обязательны только каталоги `ui/` и `interface/` у фич (как в `docs/guides/feature-structure-roadmap-ru.md`); `yarn test:e2e` — `allowedDevOrigins` в `next.config.js`, webServer на `yarn dev`, стабилизация без `networkidle`, smoke услуг через прямой `/services/expertise`, клик по hero-CTA с `force` из‑за dev-overlay Next.
- **Аудит §2.9, §5.7:** корректный `metaTitle` для публичной оферты в `data/data.json`; при ошибке `submitFeedback` в лог уходит только `correlationId` + текст (`features/feedback/utils/feedbackErrorLog.ts`).
- **Аудит §5.2–5.4 (частично §5.1):** сужены `img-src` и `connect-src` (prod) в `next.config.js`, ужесточён CSP для `next/image`; ADR [`docs/adr/0004-csp-tightening-and-trusted-svg.md`](adr/0004-csp-tightening-and-trusted-svg.md); бэклог в `docs/audit/audit-and-improvement-plan-ru.md` обновлён.
- **App Router `/documents/*`:** футер и баннер cookies больше не вызывают `next/router` через навигацию по документам — ссылки на юр. страницы через `next/link` и `@features/papers/utils/documentHref` (устранён prerender-краш `NextRouter was not mounted`).
- **Главная:** `HomePageChrome` перенесён из `pages/` в `@widgets/home-page-chrome/ui/`, чтобы не создавать лишний маршрут `/HomePageChrome`.
- **Закрытие полного аудита §1 «Несоответствие правилам» (апрель 2026):** структура `features/*` соответствует `scripts/check-feature-structure.mjs`; в `OfferBanner` добавлен CTA (`OrderButton` + проп `ctaLabel`); RU-подписи в `LeadQuiz` и `TrustSignals`; инлайн-стили вынесены в CSS-модули / CSS variables (`app/documents/*`, `ApproachCards`, `PanelButton`, `BrickWallLoader`, `UiDialog`, `Header`, `StepsWork`, `chatbotConfig`, `AppErrorBoundary`, `app/error.tsx`, Storybook `UiDialog`); в `useNews` снят глобальный `eslint-disable`, добавлен комментарий и точечный `eslint-disable-next-line`; тесты из удалённой папки `validates/` перенесены в `shared/ui/order/utils/__tests__/` и `services/__tests__/`; в правилах workflow уже зафиксирован Yarn вместо npm.

### Added

- Документ `docs/mempalace/mempalace-usage-ru.md`: проверка MemPalace после `mine`, `search`/`status`/`wake-up`, UTF-8 в Windows, цикл переиндексации.
- Каталог `docs/mempalace/` с README и `rules/01–12_*.md` — свод норм по веб-архитектуре, UI, a11y, perf, безопасности, Next.js, тестам, **чистой архитектуре** (use-case, порты), **чистому коду** (SOLID, DRY, KISS, YAGNI), ошибкам, типизации и техническому долгу для индексации в локальном дворце.
- Документ `docs/guides/yarn-package-manager-ru.md`: только Yarn, без `package-lock.json` в git; CI — `yarn audit`.
- Шаблон MCP Cursor для MemPalace: `docs/cursor/mcp.mempalace.example.json`, оглавление `docs/cursor/README.md`.
- Документ `docs/mempalace/mempalace-agent-memory-ru.md`: опциональная локальная память агента на базе [MemPalace](https://github.com/MemPalace/mempalace) (вне репозитория, без секретов); ссылка в `docs/README.md` и в `.cursor/rules/agent-context-bootstrap.mdc`.
- Документ `docs/guides/marketing-analytics-dashboard-ru.md`: карта событий `dataLayer`, формулы funnel-конверсий по шагам квиза, baseline и целевые KPI для `W4-M-01`.
- **Sprint 16 (завершение):** закрыты `M-2`, `M-3`, `M-4`; дополнительно усилен Hero по UX-замечаниям (читабельность H1 и контраст CTA).
  `M-2`: добавлен интерактивный блок `BeforeAfterShowcase` (до/после) на основе реальных проектов; **снят с главной** в `[Unreleased]` по продуктовому решению — см. раздел **Removed**.
  `M-3`: добавлен блок `TrustSignals` (метрики доверия, подтверждающие преимущества, клиентские цитаты) для усиления social proof.
  `M-4`: добавлена квиз-воронка `LeadQuiz` с пошаговым сценарием (3 шага, прогресс-бар, переход к заявке через существующий модальный флоу).
  `Hero fix`: в `HeroBanner.module.css` и `Title.module.css` устранено переполнение заголовка, усилены контраст и визуальный вес CTA-кнопки.
- **Sprint 15 (завершение):** закрыты `V-2`, `V-3`, `M-1`.
  `V-2`: главная страница получила асимметричный ритм и macro-whitespace — добавлены чередующиеся смещения секций (`pages/HomePage.module.css`) и новый секционный layout в `pages/index.tsx`.
  `V-3`: добавлены плавные scroll-reveal анимации через `useScrollReveal` с учетом `prefers-reduced-motion`; в Hero добавлены мягкие кинематографичные motion-эффекты (parallax/glow) без CLS.
  `M-1`: переработан first-screen `HeroBanner` — усилены УТП, визуальный контраст и CTA-блок; добавлены cinematic overlay/mesh слои и блок ценностных акцентов.
- **Sprint 14 (завершение):** закрыты `V-1`, `V-4`, `T-1`.
  `V-1`: внедрен акцентный шрифт с засечками `Playfair Display` (через `next/font/google`) для заголовков; настроена fluid typography (clamp) в `panda.config.ts` и `Title.module.css`.
  `V-4`: обновлена темная тема в `styles/tokens.css` — внедрен глубокий OLED-black (`#050505`) с золотыми/бронзовыми акцентами, усилены тени для глубины.
  `T-1`: начата миграция на Panda CSS — созданы базовые `textStyles` в `panda.config.ts`, `UiButton` переведен на Panda CSS recipe (`UiButton.recipe.ts`), удален `UiButton.module.css`.
- Аудит v3.0 `docs/audit/audit-and-improvement-plan-ru.md`: запущен новый стратегический план, сфокусированный на Premium UI/UX, визуальной эстетике (типографика, сетки, анимации) и маркетинговой конверсии (воронки, квизы, интерактивное портфолио).
- ADR `docs/adr/0001-ui-stack-and-design-tokens.md`: UI-стек (CSS Modules + токены), путь к опциональной миграции на HeroUI/NextUI.
- Слой токенов `styles/tokens.css` и базовые примитивы `shared/ui/primitives/` (UiButton, UiInput, UiSurface, UiTextarea, UiCheckbox) со stories в Storybook.
- Storybook 10 (`yarn storybook`, `yarn build-storybook`).
- Скрипт `yarn analyze` — отчёт `@next/bundle-analyzer` при сборке.
- [`changelog.md`](changelog.md) и регламент версий в [`README.md`](../README.md).

### Changed

- **MemPalace:** гайды перенесены из `docs/guides/` в `docs/mempalace/` (`mempalace-agent-memory-ru.md`, `mempalace-usage-ru.md`); обновлены ссылки в `docs/README.md`, `docs/cursor/README.md`, `.cursor/rules/agent-context-bootstrap.mdc`.
- **Зависимости:** `package-lock.json` убран из репозитория; в CI вместо `npm audit` — `yarn audit --level critical`; из `package.json` удалён скрипт `sync:npm-lock`.
- **Lighthouse:** русскоязычный вывод порогов/метрик и файл сводки `premier-design/.audit/lighthouse-perf-summary.json`.
- **W4-P-01 (завершение):** в CI (`.github/workflows/ci.yml`) добавлен performance gate `yarn check:perf:ci` c порогами Lighthouse mobile (score/LCP/CLS/INP/TBT) и budget initial JS для главной.
- **W4-P-01 (инструменты):** добавлены скрипты `scripts/check-lighthouse-budget.mjs` и `scripts/check-initial-js-budget.mjs`, npm-команды `check:perf:lighthouse`, `check:perf:initial-js`, `check:perf:ci`; обновлен чеклист `docs/guides/perf-and-seo-checklist-ru.md`.
- **W4-P-01 (надёжность):** прогон Lighthouse переведён на programmatic API + `chrome-launcher`, production-сервер — `node .next/standalone/server.js`; INP в lab не учитывается, если метрика отсутствует; на Windows Lighthouse по умолчанию пропускается (`PERF_AUDIT_FORCE_LIGHTHOUSE` для принудительного запуска), полный gate — в Linux CI.
- **MemPalace:** пошаговый гайд в `docs/mempalace/mempalace-agent-memory-ru.md`; шаблон `docs/cursor/mcp.mempalace.example.json` исправлен на **`python -m mempalace.mcp_server`** с `--palace` (ранее ошибочно указывалась подкоманда `mcp`, она только выводит подсказку в терминале).
- **Security headers:** `Strict-Transport-Security` и `upgrade-insecure-requests` в `next.config.js` отправляются только при `VERCEL=1` или `ENABLE_HTTPS_SECURITY_HEADERS=true`, чтобы HTTP (`next start`, CI) не провоцировал попытки принудительного HTTPS.
- **W4-M-01 (аудит):** `docs/audit/audit-and-improvement-plan-ru.md` и индекс `docs/README.md` синхронизированы с новым маркетинговым KPI-дашбордом и закрытием задачи.
- **Wave 1 (аудит v3.1, закрытие):**
  `A-01`: добавлен `check:ui-purity` (`scripts/check-ui-purity.mjs`) и интеграция в CI/lint-staged для контроля чистоты `features/*/ui`.
  `A-02`: `check-architecture-boundaries.mjs` переведен на allowlist с датой снятия долга (`expiresOn`), добавлены новые зафиксированные исключения для legacy cross-feature контрактов.
  `R-01`: принят стандарт модалок в ADR `docs/adr/0003-modal-standard-and-adapters.md` (единый контракт + legacy-adapter стратегия).
  `P-01`: `e2e/smoke.spec.ts` дополнен проверкой отсутствия hydration warnings на `/`, `/about`, `/contacts`.
  `D-02`: добавлены e2e mobile-checks на horizontal overflow (320/360/390/414) и min tap-target ключевых интерактивов; `MenuButton` увеличен до 40x40.
  `F-02`: добавлен `check:noise` (`scripts/check-noise-artifacts.mjs`), обновлены `.gitignore` (в т.ч. `.next/dev/cache`), quality-gate на шумовые артефакты включен в CI.
  `O-01`: CI-пайплайн расширен шагами `check:architecture`, `check:ui-purity`, `check:regressions`, `check:noise`; e2e-smoke дополнен базовой a11y-проверкой landmarks.
- **CI стабилизация e2e/perf (апрель 2026):**
  `e2e-smoke`: job переведен на production-подъем приложения (`yarn build` + `yarn start`) перед Playwright, чтобы исключить dev/HMR флейки.
  `playwright.config.ts`: CI-профиль детерминированный (`workers=1`, `fullyParallel=false`, повышенные timeout, `retries=1`).
  `e2e/smoke.spec.ts`: внедрен `gotoWithRetry` (retry/backoff), навигации переведены на `domcontentloaded`, проверка открытия лид-модалки усилена через ожидание полей формы.
  `check-lighthouse-budget.mjs`: добавлены retry-итерации Lighthouse и обязательная очистка временных профилей после прогона.
  `check-noise-artifacts.mjs`: добавлены ignore-пути для временных директорий Lighthouse (`.lighthouse-tmp`, `.lighthouse-chrome-profile`) для чистого noise-gate.
- **S-3 / R-2 / R-3 (завершение):** закрыты оставшиеся задачи аудита по безопасности и устойчивости UI.
  `S-3`: усилен rate limiting на публичных API — добавлен общий helper `shared/lib/applyApiRateLimit.ts` с ключом `scope:ip`, едиными заголовками (`X-RateLimit-Limit/Remaining/Reset`, `Retry-After`) и подключением к `/api/feedback` и `/api/sitemap`; `shared/lib/rateLimit.ts` расширен метаданными лимита/времени сброса.
  `R-2`: внедрен централизованный глобальный store темы (`shared/store/themeStore.tsx`), интегрирован в `pages/_app.tsx` и `app/providers.tsx`; `useThemeToggle` переведен на store + синхронизацию с `next-themes`.
  `R-3`: добавлены глобальные boundary-механизмы ошибок — `shared/ui/error-boundary/AppErrorBoundary.tsx` (pages) и `app/error.tsx` (App Router), чтобы предотвращать белый экран при сбоях UI.
- **S-3 / R-2 / R-3 (аудит):** из `docs/audit/audit-and-improvement-plan-ru.md` удалены последние открытые задачи; бэклог аудита приведен к пустому состоянию (в документе остаются только новые/будущие задачи при их появлении).
- **P-3 / P-4 (завершение):** выполнена точечная оптимизация изображений и шрифтов для Core Web Vitals.
  `P-3`: в `HeroBanner` добавлен корректный `sizes` для LCP-изображения; в `OfferBanner` убран избыточный `priority`, добавлены `sizes` и `loading="lazy"` для non-LCP баннера; в `DocumentImage` убран `priority` и включен `loading="lazy"` для второстепенных изображений документов.
  `P-4`: в `lib/interFont.ts` включены явные `preload: true` и fallback-стек (`system-ui`, `Arial`, `sans-serif`) для более стабильного рендера шрифтов и снижения визуальных скачков.
- **P-3 / P-4 (аудит):** из `docs/audit/audit-and-improvement-plan-ru.md` удалены выполненные `P-3` и `P-4`; в документе оставлены только нереализованные задачи.
- **A-4 / D-2 (завершение):** усилены архитектурные и quality-guard проверки.
  `A-4`: добавлен regression-check границ слоев `scripts/check-architecture-boundaries.mjs` + npm-команда `check:architecture`; правило блокирует новые cross-feature импорты и зависимости `shared -> features/services` (с зафиксированным legacy baseline).
  `D-2`: подключены `eslint-plugin-jsx-a11y` и `eslint-plugin-react-compiler` в `eslint.config.mjs`; правила a11y/React Compiler включены для проекта (с исключением `hooks/*` для текущего легаси-перехода); `lint-staged` расширен запуском architecture-check для `*.ts/tsx/js/jsx`.
- **A-4 / D-2 (аудит):** из `docs/audit/audit-and-improvement-plan-ru.md` удалены выполненные `A-4` и `D-2`; в документе оставлены только нереализованные задачи.
- **Sprint 13 (завершение):** закрыты `A-2`, `A-3`, `U-8`, `D-3`.
  `A-2`: запущена App Router миграция — добавлены `app/layout.tsx` + `app/providers.tsx`, маршруты документов перенесены в `app/documents/*`, старые `pages/documents/*` удалены.
  `A-3`: внедрен Server Action слой `features/feedback/useCases/submitFeedbackAction.ts`; API `pages/api/feedback.ts` переведен на вызов server action после rate-limit и валидации.
  `U-8`: добавлен headless примитив `UiDialog` на базе `@radix-ui/react-dialog`; `TextViewer` переведен на headless dialog API.
  `D-3`: добавлены Storybook истории для новых API примитивов (`UiDialog`, polymorphic `UiButton as="a"`), добавлен гайд `docs/guides/api-and-storybook-ru.md` и ссылка в `docs/README.md`.
- **Sprint 13 (аудит):** из `docs/audit/audit-and-improvement-plan-ru.md` удалены выполненные `A-2`, `A-3`, `U-8`, `D-3` и строка `Sprint 13`; в документе оставлены только нереализованные задачи.
- **Sprint 12 (завершение):** закрыты `U-3`, `U-5`, `U-6`, `U-7`.
  `U-3`: в `styles/tokens.css` добавлена единая шкала слоев (`--z-*`), хардкодные `z-index` в CSS-модулях заменены на токены по всему UI-слою.
  `U-5`: в `styles/tokens.css` добавлены dark-mode переопределения семантических токенов через `[data-theme='dark'], .dark` (совместимо с `next-themes`).
  `U-6`: `shared/ui/primitives/UiButton.tsx` переведен на polymorphic API (`as`) с `forwardRef`, без дублирования стилей для `<button>/<a>/...`.
  `U-7`: подключен `@pandacss/dev`, добавлен `panda.config.ts`, скрипты `panda:codegen` / `panda:watch`, сгенерирован `styled-system/` для токенов и utility/pattern API.
- **Sprint 12 (аудит):** из `docs/audit/audit-and-improvement-plan-ru.md` удалены выполненные `U-3`, `U-5`, `U-6`, `U-7` и строка `Sprint 12`; в документе оставлены только нереализованные задачи.
- **Sprint 11 (завершение):** закрыты `P-1`, `P-2`, `R-1`.
  `P-1`: в `next.config.js` включен React Compiler (`experimental.reactCompiler: true`) для production/dev сборок Next.js.
  `P-2`: усилен code splitting тяжелых UI-зависимостей — `EstimateModal` загружается через `next/dynamic` в `EstimateButton`, чат-движок `react-chatbot-kit` вынесен в отдельный lazy-runtime `ChatBotRuntime` (рендерится только при открытии чата).
  `R-1`: логика предварительного расчёта сметы вынесена в чистые функции `shared/ui/estimate-modal/utils/calculateEstimate.ts`, хук `useEstimateModalHandlers` упрощен и использует pure utils, добавлены unit-тесты `calculateEstimate.test.ts`.
- **Sprint 11 (аудит):** из `docs/audit/audit-and-improvement-plan-ru.md` удалены выполненные `P-1`, `P-2`, `R-1` и строка `Sprint 11`; в документе сохранены только нереализованные задачи.
- **Sprint 10 (завершение):** закрыты `A-1`, `S-1`, `S-2`, `S-4`.
  `A-1`: use-case `submitFeedback` переведен на DAL-абстракцию `services/dal/feedbackDal.ts`, внешние I/O вызовы вынесены за границу use-case.
  `S-1`: CSP-конфигурация в `next.config.js` действует в runtime c разделением policy по окружениям (dev/prod), заголовки безопасности применяются к страницам.
  `S-2`: добавлен server-side tainting слой `shared/lib/taintSecrets.ts`; секреты SMTP/Telegram маркируются в DAL (best-effort через React 19 taint APIs, без деградации runtime).
  `S-4`: сквозная валидация унифицирована на `zod`-схеме `shared/validates/feedbackSchema.ts`; клиентский `useFeedbackForm` использует ту же схему, что и `/api/feedback`.
- **Sprint 10 (аудит):** из `docs/audit/audit-and-improvement-plan-ru.md` удалены выполненные `A-1`, `S-1`, `S-2`, `S-4` и строка `Sprint 10`; в документе оставлены только нереализованные задачи.
- **Sprint 9 (завершение):** закрыты `U-4` и `U-2`.  
  `U-4`: в `FeedbackForm` добавлены связи ошибок с полями через `aria-errormessage` + `aria-invalid` для всех ключевых полей, в `CostInput` добавлены label/`aria-errormessage`/`role="alert"`, в `EstimateModal` добавлен `aria-live="polite"` для блока динамического результата.  
  `U-2`: в критичных компонентах спринта (`EstimateModal`, `CostInput`, `CollapsibleContainer`) заменены жесткие `vw/vh` на fluid-подход с `clamp()`/`min()` и относительными единицами.
- **Sprint 9 (аудит):** из открытого бэклога `docs/audit/audit-and-improvement-plan-ru.md` удалены выполненные `U-4` и `U-2`; в аудите оставлены только нереализованные задачи.
- Dev-fix CSP: в `next.config.js` `Content-Security-Policy` теперь учитывает окружение — для development добавлены `unsafe-eval` в `script-src` и `ws/wss/http` в `connect-src` для корректной работы `react-refresh`/HMR; production-политика не ослабляется.
- **D-4 (завершение):** закрыты `UI-AUD-7` и `UI-AUD-8` — для `CollapsibleContainer` вынесено поведение в `hooks/useCollapsibleContainer.ts`, для `Slider` вынесены `Arrow` в `ui/Arrow.tsx`, состояние/навигация в `hooks/useSliderState.ts`, типы стрелки в `interface/Slider.props.ts`.
- **D-4 (dead code):** закрыт `UI-AUD-10` — удален неиспользуемый закомментированный модуль `features/address/ui/YandexMap/*`.
- **D-4 (чистка проп-контракта):** закрыт `UI-AUD-4` — в `shared/ui/arrow-button/ArrowButton.tsx` удалены неиспользуемые поля и избыточный props-интерфейс.
- **D-4 (аудит):** после закрытия `UI-AUD-4/7/8/10` удалены остаточные пункты `D-4` и `UI-AUD-*` из открытого бэклога `docs/audit/audit-and-improvement-plan-ru.md` (в аудите оставлены только нереализованные задачи).
- **D-4 (продолжение):** закрыты `UI-AUD-2` и `UI-AUD-6` — для `ChatBotSidebar` вынесены props в `interface/ChatBotSidebar.props.ts` и оркестрация состояния в `hooks/useChatBotSidebar.ts`; для `FeedbackForm` вынесена форма-логика в `hooks/useFeedbackForm.ts`, добавлены типы `FeedbackForm.types.ts` и утилиты форматирования телефона `utils/phoneFormatting.ts`.
- **D-4 (аудит):** из таблицы `UI-AUD-*` в `docs/audit/audit-and-improvement-plan-ru.md` удалены выполненные пункты `UI-AUD-2` и `UI-AUD-6`; оставлены только нереализованные нарушения.
- **D-4 (частичное выполнение):** начат практический рефакторинг `ui`-слоя под новый стандарт структуры — вынесены типы из `DocumentImage` и `Papers` в `interface/*`, для `BackButton` вынесены `BackButtonProps` и навигация в `hooks/useBackNavigation`, в `Features` удален нецелевой `useMemo` и вынесен контракт `FeaturesProps`.
- **D-4 (аудит):** из таблицы `UI-AUD-*` удалены закрытые пункты `UI-AUD-1`, `UI-AUD-3`, `UI-AUD-5`, `UI-AUD-9`; в аудите оставлены только нереализованные нарушения.
- Запущен практический аудит `ui`-слоя на соответствие новым правилам: в `docs/audit/audit-and-improvement-plan-ru.md` добавлен пункт `D-4` и таблица нарушений `UI-AUD-*` (интерфейсы/утилитарная и бизнес-логика в `ui`, неконсистентные файлы, кандидаты на вынос в `hooks|interface|utils`).
- Добавлены и синхронизированы правила архитектурной дисциплины: в `.cursor/rules/agent-architecture-clean-code.mdc` зафиксированы границы слоя представления (`ui` без хуков/типов/утилит/бизнес-логики), единый нейминг и обязательный вынос переиспользуемого кода.
- Добавлен гайд `docs/guides/code-structure-and-naming-ru.md` (структура модулей, именование, правила выноса общего кода, чеклист соответствия); индекс документации `docs/README.md` обновлен.
- Аудит синхронизирован с правилом "только нереализованные задачи": выполненный пункт `D-1` удален из `docs/audit/audit-and-improvement-plan-ru.md`.
- **U-1 (верстка / a11y):** `EstimateModal`, `FeedbackModal`, `PhotoViewer` переведены на нативный `<dialog>` с `::backdrop`, нативным `Escape` (`onCancel`) и закрытием по клику вне контента; обновлены CSS-модули модалок под новый DOM.
- **U-1 (тесты + аудит):** добавлены RTL smoke-тесты `EstimateModal.test.tsx` и `FeedbackModal.test.tsx` (закрытие по backdrop и `cancel`); в `docs/audit/audit-and-improvement-plan-ru.md` удалена выполненная задача `U-1`, чтобы в бэклоге оставались только нереализованные пункты.
- Документация приведена к единому стилю: в `docs/README.md` добавлен стандарт оформления, в `docs/adr/README.md` добавлен единый шаблон ADR, в `docs/audit/audit-and-improvement-plan-ru.md` добавлен блок статуса документа и унифицирован заголовок `Definition of Done`.
- ADR синхронизированы по структуре и связям: `docs/adr/0002-no-tailwind-panda-css.md` приведен к шаблону с разделом `Проверка готовности` и ссылкой на ADR `0001`, формулировки в `docs/adr/0001-ui-stack-and-design-tokens.md` очищены от устаревшей ссылки на `ADR-002`.
- Документация и правила синхронизированы под новый UI-курс: в `docs/audit/audit-and-improvement-plan-ru.md` Tailwind заменен на Panda CSS, обновлены спринты и DoD; в `.cursor/rules/agent-architecture-clean-code.mdc` зафиксирован запрет Tailwind в новых изменениях и курс на Panda CSS + токены.
- Добавлен ADR `docs/adr/0002-no-tailwind-panda-css.md` (отказ от Tailwind, utility/recipe-альтернатива на Panda CSS), обновлен реестр `docs/adr/README.md`.
- **P2 (perf / UX dev):** `next/image` в **`ServiceDetail`** и **`RelatedServiceDetail`** — `placeholder="empty"` вместо полного URL в **`blurDataURL`** (**D-2**); слайдер Keen — обёртка **`SliderLazy`** с **`next/dynamic`**, `ssr: false`, импорты переведены с **`Slider`** (**D-4**); в **`_document`** на **`<Html>`** добавлен **`data-scroll-behavior="smooth"`** (совместимость с **`scroll-behavior: smooth`** в глобальных стилях, см. [missing-data-scroll-behavior](https://nextjs.org/docs/messages/missing-data-scroll-behavior)); в **`globals.css`** убрана лишняя **`;`** у **`z-index`**.
- **D-5:** удалена неиспользуемая зависимость **`react-multi-carousel`**; обновлён **`package-lock.json`** (`yarn sync:npm-lock`).
- **F-2 / F-4:** в **`styles/tokens.css`** добавлены **`--shadow-lg`**, **`--transition-*`**, **`--overlay-on-media`**; в ключевых карточках (`CostingCard`, `ExampleCard`, `OfferCard`, `BusinessServicesCard`, `ServicesCards`, `RelatedServiceCard`, `ReviewCard`, `ApproachCards`) — тени/переходы на токенах, **`ExampleCard`** с **`role="button"`**, **`tabIndex={0}`**, клавиатура **Enter/Space**, стили **`:focus-visible`**; ссылки и интерактив — **`:focus-visible`** / **`:focus-within`** где уместно.
- **F-2 / F-4 (вторая волна):** расширены токены (**`--overlay-scrim*`**, **`--overlay-light`**, **`--overlay-panel`**, **`--shadow-inset-soft`**, **`--color-accent-soft`**, **`--text-shadow-on-light*`**, **`--order-*`** для **`OrderButton`**); hex/rgba → токены и единые тени в **`BackButton`**, **`FeedbackForm`**, **`ServiceDetail`**, **`ServicesCards`**, **`CategoryCollapse`**, **`DocumentPage`**, **`Reviews`**, **`RelatedServices`**, **`MenuButton`**, **`CostInput`**, **`EstimateModal`**, **`FeedbackModal`**, **`PhotoViewer`**, **`Header`**, **`CookiesBanner`**, **`Features`**, **`TextViewer`**, **`OfferBanner`**, **`Title`**, **`UiButton`**; **`:focus-visible`** / **`transition`** на кнопках, ссылках и сводках (**`CategoryCollapse`**).
- **F-2 / F-4 (финал):** токен **`--shadow-banner-glow-up`** для баннера cookies; дочищены **`FeedbackModal`**, **`CookiesBanner`**; глобальные стили слайдера в **`globals.css`** (точки/стрелки на токенах, **`:focus-visible`**); **`Slider`**: стрелки **`role="button"`**, **`tabIndex`**, **Enter/Space**; **`chatbotConfig`** и **`BrickWallLoader`** — цвета через **`var(...)`**; удалён неиспользуемый **`shared/ui/slider/ui/slider.css`**.
- **E-2:** unit-тесты хуков в **`hooks/__tests__/`** — **`useLayoutProps`**, **`useScrollToElement`**, **`usePageData`**, **`useChatMessages`**, **`useCookiesBanner`**, **`usePrivacyPolicy`**, **`useThemeToggle`**, **`useCostingCardLogic`** (вместе с уже существующими **`useModalState`**, **`useFeedback`** — порог **≥ 8** хуков с тестами выполнен).
- **P2 (тесты + безопасность):** закрыт **E-3** — добавлены RTL-тесты компонентов **`MenuButton`**, **`CustomHead`**, **`OrderButton`**, **`CookiesBanner`**, **`UiButton`** (порог **≥ 5** компонентов с RTL выполнен с запасом); закрыт **G-2** — добавлен заголовок **`Content-Security-Policy`** в `next.config.js` (`headers`); закрыт **E-4** — расширено покрытие тестами для `pages/api`, `shared/utils`, `shared/hooks`, `features/*` и `shared/ui/*`, итог `statements` по `npm run test:coverage -- --runInBand`: **61.05%**.
- **P2 (регресс-контроль):** для **F-2r/F-4r** добавлен guard-скрипт `scripts/check-p2-regressions.mjs` и команда `npm run check:regressions`; подключение к `lint-staged` для `*.css` блокирует новые raw hex вне `styles/tokens.css` и новые `:hover` без `:focus-visible/:focus-within` (legacy-файлы вынесены в baseline allowlist до отдельной санации).
- **P2 (регресс-контроль, ужесточение):** baseline очищен — добавлены `:focus-visible/:focus-within` в legacy CSS-блоки и удалены все `legacy allowlist` из `check-p2-regressions`; guard теперь строгий по всему репозиторию (кроме `styles/tokens.css` и техдиров `node_modules/.git/.next/coverage/storybook-static`).
- **P2 (регресс-контроль, фиксация):** исправлен `npm run check:regressions` — скрипт теперь указывает на реальный файл `scripts/check-p2-regressions.mjs`, консистентно с `lint-staged`.
- TypeScript **6.x**, Jest **30**, `dotenv` **17**, `pg-promise` **12**; `tsconfig`: `moduleResolution: bundler`, алиасы в `paths` с префиксом `./`; для тестов — `tsconfig.jest.json` и ссылка в `jest.config.cjs`. _(Позже: без **`baseUrl`**, подавление deprecated-диагностик в Jest — **`diagnostics.ignoreCodes`** в **`jest.config.cjs`** — см. **`[Unreleased]`**.)_
- CI: в репозитории закоммичен `package-lock.json`; шаг аудита — `npm audit --omit=dev --audit-level=critical` в `premier-design/`; скрипт **`yarn sync:npm-lock`** после смены зависимостей.
- Sprint 6: таймауты **nodemailer** (SMTP) и **axios** (Telegram); абсолютные URL **og:image** / **twitter:image** и **JSON-LD logo** через `getFullCanonicalUrl`; **next/font** (Inter) в `lib/interFont.ts` + `_document`; из `_document` убраны невалидные httpEquiv cache-мета (**D-3**).
- **P1 (аудит):** каталог **`types/`** и алиас **`@app-types/*`** вместо неиспользуемого `@interface/*`; **`output: 'standalone'`** + обновлённый **`Dockerfile.prod`** (`node server.js`, без полного `node_modules`); **`docker-compose.yaml`** — убран `command`, используется CMD образа; **E-1:** шесть unit-наборов для `findService`, `findRelatedService`, `findItemByTitle`, `getCanonicalPath`, `getFullCanonicalUrl`, `generateStructuredData`; **FeedbackForm** — `UiTextarea`, `UiCheckbox`, маска телефона и select на токенах; исправлена передача **`consent`** при отправке и сообщение ошибки в **`findRelatedService`**.
- `package.json`: `resolutions` — `picomatch@^4.0.4` для единообразия дерева.
- Удалён корневой каталог `components/`: примитивы `Ui*` в `shared/ui/primitives/`, тест `FeedbackForm` в `shared/ui/order/ui/FeedbackForm/__tests__/`.
- `next/image`: устранены сочетания `priority` и `loading="lazy"` (баннер акций и др.); AVIF/WebP и `minimumCacheTTL` для кэша оптимизаций.
- Next.js **16.2** (`next`, `eslint-config-next`, `@next/bundle-analyzer`); локальный `yarn dev` в тот период — **Webpack** по умолчанию; **с 05.2026** порядок сценариев изменён: см. **`[Unreleased]`** и `package.json` (**Turbopack** по умолчанию, **`yarn dev:webpack`** — Webpack). Production `next build` по-прежнему может идти через Turbopack (см. вывод CLI). ESLint: flat-конфиг из `eslint-config-next/core-web-vitals` без `FlatCompat`; правки под правила `react-hooks` v7 (хуки, `Slider`, `OfferCard`).
- `next.config.js`: глобальные security-headers не вешаются на `/_next/*` (исключение в `source`), чтобы не ломать dev-ассеты.
- `getStaticProps` / `getData` вынесены из `pages/api/` в `lib/getStaticData.ts` (алиас `@lib/*`) — маршрут `/api/dataProvider` больше не создаётся.
- Документы: каталог `document-page`, стили `DocumentPage.module.css` (исправлена опечатка `Documet`).
- `next.config.js`: убраны no-op rewrites (`/` → `/` и т.п.); `server.js` помечен как deprecated для локального HTTPS.
- Зависимости обновлены (`yarn install`): React 19.2, sharp 0.34, `@typescript-eslint/*`, `@testing-library/dom` и др.; основной lockfile — `yarn.lock`, CI и Docker на Yarn Classic.
- Документация репозитория: [`docs/README.md`](README.md) — гайды, ADR; [`docs/audit/audit-and-improvement-plan-ru.md`](audit/audit-and-improvement-plan-ru.md) — открытый бэклог и Definition of Done.
- `tsconfig.json`: `*.stories.*` и `.storybook` исключены из типов `next build`.
- Чат-бот через `next/dynamic` с `ssr: false`.
- Production Docker: контекст `./premier-design`, `public`, `HEALTHCHECK`; dev-образ и `docker-compose.development.yaml` без лишнего `target`.
