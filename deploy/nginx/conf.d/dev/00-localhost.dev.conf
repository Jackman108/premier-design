# Запросы на localhost:8080 без имён из prod-vhost — страница-лендинг со ссылками на оба сайта.
server {
	listen 80 default_server;
	listen [::]:80 default_server;
	server_name localhost 127.0.0.1 ::1;

	root /var/www/dev-static;

	location = / {
		try_files /localhost.html =404;
	}
}
