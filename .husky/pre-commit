#!/usr/bin/env sh
set -e
# A: lint-staged (Prettier, ESLint, точечные check:* на staged) + полный typecheck — без build / risk / perf (§9 этап 2.2; полная цепочка — `yarn ci:quality` вручную или CI).
node ./node_modules/lint-staged/bin/lint-staged.js
yarn typecheck
